# STATUS.md — single-page coordination snapshot

> **Read this first at the start of every session (~30 sec).** All 3 sessions
> (A=storefront, B=admin/backend, C=mobile) update their own rows at the end
> of every working block. Append-OVERWRITE (not append-only) — keep the file
> short. For detailed history, see the three append-only contracts:
> `CONCIERGE_INSPECTION_API_CONTRACT.md`, `MOBILE_API_CONTRACT.md`,
> `V1_4_ROADMAP.md`.

**Last updated:** 2026-05-22 by Session C (after **MOBILE v0.24 + v0.25 + v0.26** — 3-wave parallel close-out of B's last 3 mobile-facing asks via 3 sonnet ruflo agents in parallel; disjoint file ownership + disjoint i18n namespaces, zero merge conflicts). **v0.24** wires `POST /v1/public/me/sell-bookings/:bookingRef/cancel` (closes own [ASK C→B] sell-bookings-cancel-endpoint from v0.23): cancel modal un-commented + real mutation with Idempotency-Key, 3 typed errors (NotFound/NotCancellable/Validation), `buildInspectorInfo()` migrated to v1.5.14's richer inspector shape (fullName/initials/whatsappE164/rating/completedCount) with legacy fallbacks, report-CTA decision tree (`relatedOfferToken` → /offer/:token/inspection-report; else `inspectionReportPdfUrl` → expo-web-browser; else disabled), `CancelledChip` rendered when `cancelledAt !== null`. `overallScore` badge deferred to v0.27 housekeeping per >20-line guard. **v0.25** new `<RichMediaSection />` (160 lines) on VDP: extended `PublicListingDetailSchema` w/ `walkaroundVideo` + `spin360` (both nullable+optional, bare z.string() for relative `/static/` paths), installed `react-native-video` via expo, `<Video controls paused>` walk-around at 16:9 with poster, 360° sub-block stubbed pending slider package, both sub-blocks individually null-guarded, `absUrl()` helper resolves against `EXPO_PUBLIC_API_URL`. **v0.26** defense pass closing B v1.5.15 [ASK B→C]: reusable `<ImageWithFallback />` wrapper (54 lines, `brand[100]` bg + `<CarSilhouette />` on error), drop-in replacement at 4 sites: ListingCard hero (browse+favorites+home), VDP PhotoGallery (hero slides + thumbs), SimilarCarsRail card, HeroCard avatar (inline guard falling back to initials text). Final gates: TSC GREEN (apps/mobile clean), i18n EN 1108 / AR 1108 — symmetric 0 missing, brand-lock PASS. Arabic `الضمان` remnant in ar.json:725 confirmed legitimate "warranty" doc-type, NOT a missed Al Daman scrub. **All 3 [ASK B→C] + own [ASK C→B] closed. Mobile fully feature-complete for v1.5 launch.** §13.5 ops gates for user: prisma:migrate (if not applied) + nx serve api restart + `expo prebuild --clean` (re-link react-native-video native code) + 2 smoke probes (POST cancel + VDP rich-media render).

**Prior:** 2026-05-21 by Session B (after **B v1.5.20** — Al Daman → Behbehani brand scrub closes C's brand-correction directive from v1.5.19's Prior. **B-domain scope**: 3 occurrences in 2 files. (1) `apps/admin/src/app/features/listings/edit/listing-edit.component.html` lines 225 + 231 — empty-state captions "Al Daman integration lands later" → context-appropriate Behbehani phrasing ("Score posts after the Behbehani inspector completes the rubric" / "Pending Behbehani inspector assignment"). (2) `mockups/admin/sprint-4-inspection/_shell.html` line 5 — historical header comment "(Al Daman is OUT)" → "no third-party vendor involved". `nx build admin` GREEN. B-domain greps now clean across apps/admin/**, apps/api/**, mockups/admin/**. **Repo-wide remaining**: 5 A-domain files in mockups/web/sprint-3/** (already on A's plate per C's STATUS note — not touched per file ownership) + STATUS.md historical text (audit trail, intentionally preserved). **Drive-by flag**: `الضمان` Arabic remnants found in apps/web/.../ar.json + apps/mobile/.../ar.json — could be legitimate Arabic "guarantee/warranty" microcopy OR missed scrubs; A + C to sanity-check. Posted v1.5.20 ack to CONCIERGE with full file:line table.)

**Prior:** 2026-05-21 by Session B (after **B v1.5.19** — Rich-media demo content upgrade. User flagged v1.5.16's synthetic placeholders (brand-blue text slide + cartoon spin) as "not good for a demo". Replaced with REAL CC0-licensed content: **(1) Per-car personalized walk-around** — each of 6 premium listings now has its own 7-sec Ken-Burns animation built from its actual Unsplash hero photo via ffmpeg `zoompan` filter. Customer sees the same car on VDP video as on the listing card. **(2) Real 360 turntable footage** — downloaded Pixabay video #2708 "Car Red Rotate" (CC0 Pixabay Content License, commercial-OK no-attribution), 2.3 MB at 1280×720 medium variant. Shared across all 6 premium listings (per-car turntable shoots need a physical $5k-15k rig — flagged as eventual prod replacement via existing admin upload UI). **Drive-by fix**: Patrol hero photo URL `photo-1606664922998-f180baa4ef91` was 404'd by Unsplash sometime post v1.3 (broken image on listing card); swapped for stable `photo-1485463611174-f302f6a5c1c9`. Other broken hero URLs in seed (Mustang 0006, 0011, 0012) flagged as carry-over (not v1.5.19 scope). **Repo footprint**: ~6.4 MB net (6 walkarounds ~3.5MB + 6 posters ~650KB + 1 spin360 2.3MB). Source photos gitignored under `apps/api/src/seed/demo-media/source-photos/`. **DTO impact: NONE** — same `walkaroundVideo` + `spin360` shapes; only `url` values change. A's v1.5-D12 VDP player components keep working unchanged. **Smoke evidence**: 13/13 assets serve 200 + correct content-type + traversal blocked. `nx build api` GREEN. **Operational gate for A**: `npm run db:seed` to overwrite v1.5.16 demo rows with new per-car URLs. **C's Al-Daman scrub ASK noted** — flagging B-domain 2-file scrub (listing-edit.component.html + sprint-4-inspection/_shell.html) as next pick; brand pattern "Behbehani inspected" / "مفحوصة من بهبهاني" already locked by C.)

**Prior:** 2026-05-21 by Session C (brand-correction in-thread, user directive: **"inspection is done by Behbehani, not by Al Daman — don't mention it anywhere"**. C-domain swept clean: 5 mobile RN files (i18n EN+AR + FilterSheet + VdpTitleBlock + InspectionReportEmbed — renamed keys `alDaman*` → `behbehani*`, EN/AR copy updated to "Behbehani inspected" / "مفحوصة من بهبهاني" etc.) + 4 mobile mockup HTML files + shared `memoryfile.md` (3 historical refs corrected: vendor list, integration-mode question, out-of-scope §3.6 note). TSC clean. **⚠️ Cross-session cleanup needed — 7 files still mention "Al Daman" in A's and B's domains, please scrub on next session start:** A-domain (5): `mockups/web/sprint-3/{01-home-en,03-listings-grid,04-listings-list,05-vdp}.html` + `mockups/web/sprint-3/behbehani-motors/car-market/project/srs_extracted.txt`. B-domain (2): `apps/admin/src/app/features/listings/edit/listing-edit.component.html` + `mockups/admin/sprint-4-inspection/_shell.html`. Brand pattern: EN "Behbehani inspected" / AR "مفحوصة من بهبهاني" — matches mobile + memoryfile.

**Prior:** 2026-05-21 by Session B (after **B v1.5.18** — `/v1/public/listings/featured` filter fix shipped, closes A's `[ASK A→B-5]` v1.5-D11f. A diagnosed it exactly: the endpoint was ignoring the admin-controlled `featuredAt` flag and effectively returning "8 most-recent listed + inspected-first sort", so admin PATCH toggles had zero effect on the customer home-page rail. Applied A's proposed 3-line fix verbatim: added `featuredAt: { not: null }` filter + `orderBy: [{ featuredAt: 'desc' }]` + dropped the in-memory sort + consolidated cap from 32+slice(8) to take 8 directly. Also extended `seedDemoRichMedia()` to set `featuredAt = now() - (index hours)` on the same 6 premium listings already wired for rich media (Porsche/BMW/Nissan/Mercedes/Audi/Lexus) — keeps the rail populated out of the box AND establishes the v1.5 mental model: **premium tier = featured + rich media**. 2 files touched (listings-public.controller.ts + seed.ts), zero new migrations, `nx build api` GREEN. Operational gates for A: `nx serve api` restart + `npm run db:seed` to populate the 6 featured rows. Posted v1.5.18 ack to CONCIERGE with full behaviour matrix (4 scenarios) + verification checklist. B residual: idle, zero open coordination items on B's side.)

**Prior:** 2026-05-21 by Session B (after **B v1.5.16** — Listing rich media (walk-around video + 360° exterior spin) now surfaced on customer-facing VDP. Backend was 100% pre-built (`ListingVideo` + `Listing360` Prisma models, full admin CRUD with audit, admin upload UI). Real gap: `GET /v1/public/listings/:slug` explicitly stripped video/media360 so even admin-uploaded media never reached customers. Fix: extended `ListingPublicDetail` with two null-safe fields (`walkaroundVideo`, `spin360`) + extended `DETAIL_INCLUDE` with selects + `toPublicDetail` with mapping. New static mount `/static/demo-media` (via `express.static`, immutable 7-day Cache-Control) serves demo MP4s committed under `apps/api/src/seed/demo-media/` (~210 KB total, copied into `dist/seed/demo-media/` by extended webpack assets entry). **Demo content** generated locally by new `scripts/generate-rich-media-demo.mjs` using `ffmpeg-static` + `sharp` (no internet crawl needed; copyright-clean synthetic placeholders): 6-sec branded walk-around MP4 (50 KB) + 28 KB poster + 3-sec 36-frame 360 spin MP4 (130 KB). Seeded into 6 premium listings (Porsche Cayenne, BMW X5, Nissan Patrol, Mercedes C 300, Audi Q5, Lexus RX 350) via new `seedDemoRichMedia()` step (idempotent — filters on `/static/demo-media/` prefix so real admin uploads are never touched). Smoke probe via `scripts/smoke-demo-media.mjs`: 200 + correct content-length on all 3 assets + traversal probe `/../../package.json` returns 404. **Coordination posted**: `[ASK B→A]` in CONCIERGE v1.5.16 for web VDP video + 360 player (~1.5–2h estimate); `[ASK B→C]` in MOBILE v0.23-B-reply-3 for react-native-video + scrubber 360 viewer (~1h estimate). Both non-blocking — fields are optional/null-safe so old clients keep working. **Operational gates**: user runs `npm run db:seed` to populate 6 premium demo rows (no migration needed — schema was already in place). Lead-verified `nx build api` GREEN + dist assets present + smoke probe PASS.)

**Prior:** 2026-05-21 by Session C (after **MOBILE v0.23** — `[ACK]` B v1.5.13 closing both v0.22 asks (sell-bookings GET/GET/PATCH + ConciergeBookingStatus.inspector field). Big revelation in B's investigation: B had NO `/v1/public/me/sell-bookings/*` routes before — mobile's v0.22.b client would have 404'd on every call. Now wired correctly. **Mobile-side tracker UI gating shipped in-thread**: Reschedule button disabled when status≠draft (pre-empts B's 409 BOOKING_NOT_RESCHEDULABLE) + Cancel button shows "Contact support" alert (no DELETE endpoint exists yet → 1 new `[ASK C→B] sell-bookings-cancel-endpoint` posted, non-blocking). 6 new i18n keys (rescheduleLocked + cancelContactSupport). Final: EN 1095 / AR 1095 across 28 namespaces; brand-lock 0 across 216 files; TSC clean. **User needs API restart** to load v1.5.13 mount + smoke /me/sell-bookings.)

**Prior:** 2026-05-21 by Session A (after **A v1.5-D6** — /account/* settings-shell rebuild: replaced 13-tile hub + per-page hero+back-link with **persistent left sidebar (desktop) + horizontal pill scroll (mobile) + content pane** wrapping all 14 account sub-pages. **(a) NEW** AccountLayoutComponent + 3 sub-components (account-nav shared data + sidebar-desktop + sidebar-mobile-pills). **(b) Routes restructure** — /account becomes parent route with 14 nested children (profile/addresses/notifications/security/documents/orders/orders/:id/saved-searches/inspections/favorites + 5 coming-soon); /account → /account/profile redirect; legacy /my-bookings URLs preserved via top-level redirects. **(c) Strip hero+back-link** from 12 child pages (class logic — signals/computed/validators/save handlers/polling/countdown/cancel — ALL preserved). **(d) DELETE** account-hub.component.ts (tile-grid retired). **(e) In-thread** — removed duplicate "Profile" item from header avatar dropdown. **i18n**: +26 keys `account.shell.nav.*` (17) + `account.shell.page.*` (9). 4 groups (PROFILE & SETTINGS / BUYING / OWNING / ENGAGEMENT) match prior hub. Coming-soon items get neutral SOON badge (NOT amber). Active state `bg-brand-50 text-brand-700`. Sign-out at bottom (red destructive). Browser-verified: /account → profile redirect, sidebar persists across cross-route clicks (zero remount). Build + brand-lock + i18n-parity GREEN.)

**Prior:** 2026-05-21 by Session A (after **A v1.5-D5** — `/sell/concierge` 3-step wizard + tracker FULL v2 mockup rebuild via 2 parallel ruflo agents (opus). User flagged mockup-fidelity gap after v1.5-D4 hero canonicalization. **(a) Wizard rebuild** — concierge-page.component.ts (582→494 lines) + 4 new step/success sub-components + 2 new shared components (address-autocomplete + date-strip) + new address-suggestion.service.ts adapter. Hero trust strip (3 brand-blue pills, NOT emerald per brand-lock). Step 1: 2-card layout (Address + Schedule) with typeahead autocomplete + collapsed parking notes + 14-day horizontal scroll strip + 3-up time-window cards (SVG icons, no emoji/slot-badges). Step 2: privacy trust micro-strip. Step 3: promise block + 4 review cards. Browser-verified live. **(b) Tracker rebuild** — concierge-status-page.component.ts (403→499 lines) + 2 new sub-components (tracker-timeline + tracker-inspector-card). BookingRef anchor chip + copy-button. 4-step timeline visualization. Inspector card (gradient avatar + brand-700 star, NOT amber). 3-up quick actions (calendar ICS + reschedule + cancel red-destructive). Signed-state hero kept brand-blue (NOT emerald) per v1.5-D1 lesson. **(c) Brand-lock translations** — emerald/amber from mockup → brand-blue / slate / red-600. **(d) In-thread fix** — tracker `reportLink()` was wired to non-existent route; changed to null + new disabled CTA with "Report available with your offer" copy (lights up when B extends DTO). 75 new EN+AR i18n keys. 5 guards GREEN. 2 new `[ASK A→B]` (DTO extensions + cancel endpoint) + 1 new `[GATE]` (GOOGLE_MAPS_API_KEY for autocomplete real-mode; adapter ships with mock KW seed today).)

**Prior:** 2026-05-21 by Session C (after **MOBILE v0.19** — `[ACK]` B v1.5.6 same-cycle. B shipped all 3 backends (notifications + maintenance + reviews = 14 endpoints + 3 migrations); C spawned 3 sonnet agents in parallel to wire mobile surfaces. **v0.19.a Notifications** — MeNotificationsApiClient + /notifications route + unread badge hook (30s staleTime). **v0.19.b Maintenance** — MeMaintenanceApiClient + 3 routes incl. modal create form + governorate snake_case wire (`mubarak_al_kabeer` NOT camelCase per B's deviation). **v0.19.c Reviews** — 2-client split (auth + no-auth via rawHttpClient for public listing-reviews) + /reviews route + VDP ReviewsSection + StarRatingInput (brand-blue, NO yellow) + reviewability-gate 403/409 inline handling. 3 more account-hub tiles fixed — only Financing + Referrals remain Coming-Soon by design. EN 927 / AR 927 across 24 namespaces; TSC clean; brand-lock 0 across 189 files. All 3 prior asks CLOSED. Mobile fully idle pending user §13.5 ops (prisma:migrate + API restart for smoke).)

**Prior:** 2026-05-21 by Session A (after **A v1.5-D** — sell-flow close-out: (a) **offer-page emerald gradient FIX** — accepted-state hero kept brand-blue; differentiation via inner checkmark+copy, not color swap. (b) **brand-lock guard extended** — BANNED_PREFIXES adds `from`/`via`/`to` so future gradient-stop regressions fail the gate. (c) **brand-logo `(error)` fallbacks** in 5 component files including shared `ui-select.component.ts` — broken favicon URLs now render initial-letter chip in `bg-brand-100 text-brand-700` instead of broken-image icon. (d) **NEW `/offer/:token/inspection-report` page** — 371-line Angular component (13.98 kB lazy chunk), brand-lock-compliant (no green; score badges use brand-blue bands + red-600 only for <50), 33 new EN+AR i18n keys symmetric, "View inspection report" CTA wired in active/countered offer view only. One `[ASK A→B]` posted for thin `GET /v1/public/concierge/offers/:token/inspection-report` endpoint — A side renders `not_found` gracefully until B ships. Build + 5 guards GREEN.)
**Bootstrap convention:** see `CONVENTIONS.md` for tags + sync ritual.

---

## 🟢 Live now — customer-facing surface

| Stack | What's live | Owner |
|---|---|---|
| Web storefront (apps/web, port 4200) | Home · Browse + Save-this-search CTA + modal · VDP + Reserve flow (checkout modal → Otto → return-page polling) · Sell wizard (3-step) · Sell concierge + tracker · Offer view/counter/accept/decline/expired · Sign-in modal · Sign-up modal · Inspection-sign page (token URL) · Account hub (13-tile, 4 groups) · Profile · Addresses · Notifications (3×4 grid) · Security (sessions + sign-out-all) · Documents (real, paginated, kind-filtered, signed S3 URLs) · Orders (list + dedicated detail page w/ countdown + cancel) · **Saved Searches (real list + browse-CTA flow, replaces Coming-Soon shell)** · 5 Coming-Soon shells remaining (Maintenance / Financing / Returns / Reviews / Referrals) · My bookings · Saved cars | A |
| API public surface (apps/api) | /v1/public/auth/* (register, login, logout, refresh, OTP issue/verify, Google verify) · /v1/public/me/* (22+ endpoints: profile, addresses, notification-preferences, sessions, sign-out-all, inspections, saved-listings, documents v1.4) · /v1/public/offers/:token/* (4) · /v1/public/sell-bookings (3) · /v1/public/feature-waitlists · /v1/public/notifications/push-token (mobile) · /v1/public/orders (create/cancel/payment-init) · /v1/public/me/orders (list/detail) · /v1/public/payments/otto/callback (Otto webhook, HMAC-verified, mock-fallback) · **/v1/public/me/saved-searches (5 endpoints: list/get/create/update/delete — v1.5-A)** | B |
| Mobile native (apps/mobile, Expo SDK 52) | Home · Browse · VDP · Sign-in · Sell wizard (3-step) · 5 offer-state screens · Inspection report viewer · Inspection-sign deep-link bouncer · Account v2 hub (in flight) · Expo push SDK + token capture | C |
| Admin (apps/admin) | CPO inventory pipeline · concierge bookings review · offers admin · admin Documents UI (live — /customers/:id/documents, list+filter+upload via 3-step pre-signed S3 flow) · admin Orders queue (live — /orders list+filter, /orders/:id detail with status-update + cancel panels) | B |

---

## 🟡 In flight per session

| Session | Sprint | Working on | ETA | Owns these files |
|---|---|---|---|---|
| **A** | v1.5-D11 done | **idle** — v1.5-D11 just shipped home page polish + 4 broken click-group fixes via ruflo opus agent (~12 min, 45 tool calls). **Diagnostics**: Featured Cars Search button + brand circles + body tiles + hero CTAs were ALL dead `<button>` elements with no `(click)` handler (hero CTAs were stub methods with TODO comments). **Fixed**: each now uses `<a routerLink>` with `?brand=`/`?body=`/`?budgetMaxKwd=` queryParams matching /browse-page convention. Hero CTAs navigate per-slide via Router + LanguageService. **Polish (4 dims)**: bigger trust chips (icon+title+sub cards) + new 3-stat social-proof row (1,200+ cars / 97% sat / 200K+ inspections) + subtle grid bg overlay; brand letter-chips polished (gradient bg + bold initials + shadow — agent chose letter-chip path over bundled SVGs for trademark safety); 8 distinct body silhouettes (per-type wheelbase + accents); sticky search bar (`top-[80px] z-30 backdrop-blur`) + filter chip preview row with X-buttons + `active:scale-[0.98]` tap feedback. +11 i18n keys EN+AR symmetric. Browser-verified live. **B v1.5.14 shipped mid-cycle** — both [ASK A→B-2] + [ASK A→B-3] closed (consolidated with C's overlap). Wire-up = v1.5-D12 next pick (apply prisma:migrate + rebuild shared-types + wire cancel button against `/v1/public/me/sell-bookings/:bookingRef/cancel` + verify inspector card / score / report CTA auto-render). 3 new follow-ups spotted from D11: /finance + /services routes don't exist (placeholders) + sell-callout 3 buttons still dead. | — | `apps/web/**`, `libs/shared/types/src/lib/{*.public.schemas,offer-public,offer-respond-public,document.public,push-token.public,me-*}.ts` |
| **B** | v1.5.22 done → idle | **idle.** Latest: v1.5.22 repaired 3 broken seed hero URLs (Mustang/BMW/Land Cruiser) — all 12 seed heroes now return 200, browse grid is image-clean. v1.5.21 fixed browser-cache trap on 360 spin (rename + Cache-Control loosen). v1.5.20 closed C's Al-Daman scrub on B-domain (2 files). v1.5.19 rich-media demo content upgrade (per-car Ken-Burns walkarounds + real CC0 Pixabay 360 turntable). v1.5.18 `/featured` filter fix closes A's v1.5-D11f ask. Endpoint now honours admin-controlled `featuredAt` flag. Seed extended so same 6 premium listings get `featuredAt` alongside rich media (premium = featured + rich media). v1.5.17 deployment runbook also fresh in repo (`docs/DEPLOY.md` + 5 tracked artifacts: nginx config, PM2 ecosystem, env example, deploy script, proxy snippet). Zero open coordination items on B's side. **Standing by**: Otto / PACI / APNs / Apple creds (long-running gates); A's web img-fallback coverage + C's mobile Image-onError coverage (v1.5.15 carry-over, non-blocking); user's EC2 deployment when ready (runbook in `docs/DEPLOY.md`). | standby | `apps/api/**` (except `*.public.schemas.ts` A owns), `apps/admin/**`, all `prisma/**`, `libs/shared/types/src/lib/admin-*.ts` |
| **C** | v0.24+v0.25+v0.26 done → idle | **All 3 B-side mobile asks closed in single 3-agent parallel wave** (tracker-wirer 55 calls + media-wirer 27 + img-fallback-wirer 27, disjoint scope/i18n, zero merge conflicts). v0.24 wires POST cancel + inspector-DTO migration + report-CTA decision tree + CancelledChip. v0.25 ships `<RichMediaSection />` on VDP w/ react-native-video + 360 stub + extended `PublicListingDetailSchema`. v0.26 ships reusable `<ImageWithFallback />` at 4 user-uploaded photo sites (defense-in-depth). Final: TSC GREEN, EN 1108 / AR 1108 across 28 namespaces, brand-lock PASS. Arabic `الضمان` confirmed legitimate warranty doc-type. **Mobile fully feature-complete for v1.5 launch.** Only user-driven items remain (Otto/APNs/Apple/bundleId/Huawei v1.6+). User §13.5 ops gates: `prisma:migrate` (if not yet) + API restart + `expo prebuild --clean` (re-link react-native-video) + smoke POST cancel + smoke VDP rich-media on premium seeded listing. | idle | `apps/mobile/**`, mobile mockups under `mockups/sprint-M{1,2}/` |

---

## 🚦 Blocking (cleared regularly)

| Blocker | Owner | Blocks who | Since | Notes |
|---|---|---|---|---|
| ~~`[BLOCK-API]` API on :3000 returns 500 on /auth/login + 2 pending Prisma migrations~~ | ~~User~~ | ~~A's smoke walk~~ | ~~2026-05-20 v1.4.8~~ | **CLEARED 2026-05-20 v1.4.9** — user ran migrations + started API (`/v1/auth/login` works, returns JWT). Smoke walk done. |
| `[BLOCK-HUSKY]` husky pre-commit hook can't install — repo not in git (`git init` required) | User | none (guards still runnable as `npm run guard:all`) | 2026-05-20 v1.4.6 | One-time user action. Per CLAUDE.md, agents don't run `git init` unilaterally. After `git init` + `git add -A && git commit -m "initial"` + `npx husky install`, the guards become enforcing. |
| ~~`[BLOCK-CI]` brand-lock guard fails — 45 pre-existing amber/emerald violations~~ | ~~A~~ | ~~none~~ | ~~2026-05-20 v1.4.6~~ | **CLEARED 2026-05-20 v1.4.8 — 0 violations** (v1.4-A10 fixed 29 in sell+vdp; v1.4-A12 fixed remaining 16 in auth+inspection-sign+home+sell/concierge). `npm run guard:brand-lock` → ✔ exits 0. |

---

## 📋 Open asks (cross-session)

| Tag | Ask | Status |
|---|---|---|
| `[ASK B→A]` B-A-1: Add "agent ship checklist — menu wiring required" rule to CONVENTIONS.md | **CLOSED** in v1.4.8 — CONVENTIONS.md §13 "Agent ship-checklist" added with explicit menu-wiring + route-table + nav-shell-update requirements for every new feature surface. Both A and B agents are now bound by the same checklist. | ✅ |
| `[ASK A→B]` v1.5-D §5 — `GET /v1/public/concierge/offers/:token/inspection-report` | **CLOSED** in **B v1.5.7** (~75 net lines, no migration). A's `OffersService.getInspectionReport$` resolves to real data on next `nx serve api` restart. ✅ |
| `[ASK A→B-4]` v1.5-D7 §3 — POST avatar upload endpoint | **CLOSED** in **B v1.5.10** + A wired in v1.5-D8 (3-step S3 flow + spinner + 5MB/mime guards + 4 toast states). ✅ |
| `[ASK A→B-2]` v1.5-D5 §5 + v1.5-D10 §2 — Extend `ConciergeBookingStatusSchema` with `inspector + overallScore + inspectionReportPdfUrl + relatedOfferToken + cancelledAt` | **CLOSED** in **B v1.5.14** — schema extended with A's richer inspector shape + legacy aliases. PDF signed via new `presignGetUrl` (15-min TTL). Tracker `reportLink()` decision-tree auto-lights up post shared-types rebuild. Wire-up tracked as A v1.5-D12. ✅ |
| `[ASK A→B-3]` v1.5-D5 §5 + v1.5-D10 §3 — `POST` booking-cancel endpoint | **CLOSED** in **B v1.5.14** — mounted at `/v1/public/me/sell-bookings/:bookingRef/cancel` (me-router for cleaner auth boundary; B offered to add A's path as alias if preferred). Idempotent + state-machine guard + inspector notification on cancel. Wire-up tracked as A v1.5-D12. ✅ |
| `[ASK A→C]` | _(none open)_ | |
| `[ASK C→A]` A-1 `previousPriceFils` + A-2 `PublicListingDetailSchema` | **CLOSED** in v1.4.5 §6; **WIRED in C v0.10** (ListingCard + listings-public.client + vdp.types) | ✅ |
| `[ASK C→B]` B-C-3..B-C-8 (push provider routing, schemas, Otto race, signed-URL TTL) | **CLOSED** in MOBILE v0.10-B-reply (2026-05-20). B-C-3 APNs direct confirmed · B-C-4 `[ACK-REJECT]` 9-category enum (use deep-link routing instead) · B-C-5 use `push-token.public.schemas.ts` (**WIRED in C v0.11**) · B-C-6 polling pattern (deferred to Day 5+) · B-C-7 409 confirmed (deferred to Day 8+) · B-C-8 15-min initiation TTL only | ✅ |
| `[ASK C→A]` schema-3: delete orphan `device-token.public.schemas.ts` | **CLOSED** in v1.4.8 — file deleted, barrel re-export removed, dist artifacts purged, `nx build {shared-types,web}` GREEN. | ✅ |
| `[ASK C→B]` C-B-1 (v0.22.b) — `/v1/public/me/sell-bookings/*` route family. Mobile's `MeSellBookingsApiClient` calls 4 endpoints (GET list, GET :ref, PATCH :ref reschedule, DELETE :ref cancel). | ✅ **CLOSED** in B v1.5.13 (GET/GET/PATCH) + B v1.5.14 (POST cancel at `/me/sell-bookings/:ref/cancel`) + **WIRED in C v0.24** (typed errors, idempotency, mutation + tracker refetch). |
| `[ASK B→C] rich-media-render` (v1.5.16) — RN VDP walkaround video + 360 spin | ✅ **CLOSED in C v0.25** — `<RichMediaSection />` on VDP, react-native-video installed, schema extended, EN/AR i18n shipped. 360 sub-block stubbed pending slider package (~3h follow-up if real-360 needed pre-launch). |
| `[ASK B→C]` (v1.5.15) — RN Image onError fallbacks | ✅ **CLOSED in C v0.26** — reusable `<ImageWithFallback />` at 4 user-uploaded photo sites (ListingCard / VDP PhotoGallery / SimilarCarsRail / HeroCard avatar). |
| `[ASK C→B]` C-B-2 (v0.22.b) — Extend `ConciergeBookingStatusSchema` with inspector PII when assigned. | ✅ **CLOSED** in B v1.5.13. B shipped `inspector: { name: string; phoneE164: string \| null } \| null` (PII-light — no rating, no initials, no completedCount, no assignedAt; B notes those are future enhancements). **WIRED in C v0.22.b-2** (`buildInspectorInfo()` in tracker maps B's shape → InspectorCard's InspectorInfo, deriving initials/firstName client-side and defaulting rating="5.0"/completedCount=0 since B's DTO omits them). |
| `[ASK B→C]` _(none open)_ | | |

---

## 🚪 Gates (must verify before crossing)

| Gate | Verifier | Status |
|---|---|---|
| `[GATE]` v1.3 customer-account smoke walk | A (Chrome MCP + screenshots) | ✅ structural verified in v1.4.5; signed-in walk still pending test creds |
| `[GATE]` v1.4 Otto integration (sandbox creds + merchant portal config) | User → B | ⏳ pending user action |
| `[GATE]` Apple Sign-In v1.5 (Apple Developer + App Store §4.8 lead time) | User → C → A | ⏳ pending user (App Store §4.8 = 6-week lead) |
| `[GATE]` Firebase service account for FCM push (Android) | User → B | ✅ **CLEARED 2026-05-20 v1.5.1** — JSON at `apps/api/.secrets/firebase-admin.json`, `FIREBASE_SERVICE_ACCOUNT_PATH` env set, `firebase-admin@^13.10.0` installed, `push.adapter.ts` lazy-init calls `cert(path)`. FCM auto-switches mock→real on first dispatch. |
| `[GATE]` APNs `.p8` + KEY_ID + TEAM_ID + BUNDLE_ID for push (iOS) | User → B | ⏳ pending — iOS push tap routing (C #64) blocked on E2E verify until landed |
| `[GATE]` `GOOGLE_MAPS_API_KEY` for /sell/concierge address autocomplete + map preview | User → A | ⏳ pending — A side shipped v1.5-D5 with adapter pattern (mock KW seed of ~15 addresses today). Auto-switches mock→real on first load when `window.google?.maps?.places` is detected. Provision via console.cloud.google.com → enable Places API + Maps JavaScript API + restrict by referrer to `*.behbehanimotors.com` and `localhost:4200`. |
| `[ON-HOLD]` Documents approval queue / Customer KYC / Insurance / Otto payment integration | User (external API creds) | ⏸ parked per stakeholder 2026-05-20 — revisit when creds available (PACI for KYC, Otto for payments, separate vendors for Insurance + Documents) |

---

## 🎯 Recently shipped (last 7 days)

- **2026-05-21 B v1.5.23 — IP-only deployment helper + DEPLOY.md appendix** `[SHIPPED 2026-05-21 B v1.5.23]` — User confirmed pure HTTP on EC2 IP for v1 (no domain yet). Shipped `scripts/setup-env-ip-only.sh` (~180 lines) — interactive bash helper that prompts for the 5 IP-only values (EC2 IP, RDS endpoint+password, ElastiCache endpoint, S3 bucket, AWS region), auto-generates JWT secrets via openssl, and writes `apps/api/.env` (chmod 600). Supports both interactive prompts AND non-interactive flag mode for CI use. Validates IPv4 format, enforces JWT secret inequality, refuses overwriting existing `.env` unless `--force`. Added 130-line **Appendix — IP-only HTTP deployment** to docs/DEPLOY.md: constraints matrix (what works ✅ / what breaks ❌), modified Phase 1 steps (skip Route 53, certbot, change nginx server_name to `_`), helper script usage walkthrough, IP-only smoke probes, upgrade path → real domain + TLS (15-min path, no data loss), upgrade path → CloudFront-fronted HTTPS (free `*.cloudfront.net` cert, no domain needed). `bash -n` syntax verified clean. Reduces step §1.3 of DEPLOY.md from ~15 min manual editing to ~2 min guided prompts. B residual: idle.
- **2026-05-21 B v1.5.22 — Repaired 3 broken seed hero URLs (Mustang/BMW/Land Cruiser)** `[SHIPPED 2026-05-21 B v1.5.22]` — Carry-over flagged in v1.5.19: 3 of 12 seed listings (`BMC-SEED-0006/0011/0012`) had Unsplash photo URLs that 404'd post-v1.3 → broken images on customer browse grid. Discovered + verified via curl HEAD scan during v1.5.19. **Replacement strategy**: extracted real CDN URLs via Unsplash's `/photos/{slug}/download` 302 redirect (works around their bot-block on page scraping). All replacements verified-stable. **Picks**: (0006) Mustang GT → "Parked red Ford Mustang muscle car" by Stella de Smit; (0011) BMW 330i → "White BMW sedan" by Alvis Taurēns; (0012) Land Cruiser VXR → "Grey Toyota Land Cruiser on desert during daytime" by Kristina (real Land Cruiser in desert setting — matches Kuwait brand context; color is grey not Dune Beige spec, but model fidelity > exact color match). **Verification**: re-scanned ALL 12 seed hero URLs post-edit — 12/12 return 200 (was 9/12 before). Seed.ts typecheck clean. 1 file touched, 3 edits, no migration. **B residual**: idle, zero open coordination items. **Carry-over from v1.5.19 thinned**: no more known-broken hero URLs in seed.
- **2026-05-21 B v1.5.21 — Browser-cache trap on 360 spin (user-flagged)** `[SHIPPED 2026-05-21 B v1.5.21]` — User reported v1.5.19's Pixabay 360 didn't appear in browser despite file swap. Root cause: URL stayed identical (`/static/demo-media/spin360/demo-spin360.mp4`) while content changed, AND express.static was emitting `Cache-Control: immutable max-age=7d` — browsers kept the v1.5.16 cartoon bytes locally. Two-pronged fix: (a) renamed file to `demo-spin360-v2.mp4` + updated `SPIN360_URL` in seed.ts → new URL forces cache miss; (b) loosened Cache-Control to `max-age=3600, must-revalidate` (dropped `immutable`) so any future demo swap goes live within 1 hour. 5 files touched. README documents the `-vN` convention for future content swaps. nx build api GREEN, 13/13 smoke checks PASS.
- **2026-05-21 B v1.5.20 — Al Daman → Behbehani brand scrub (B-domain complete, closes C's v1.5.19 directive)** `[SHIPPED 2026-05-21 B v1.5.20]` — C posted brand-correction directive ("inspection is done by Behbehani, not by Al Daman — don't mention it anywhere"); C scrubbed C-domain (5 mobile RN files + 4 mobile mockups + memoryfile), then flagged 2 B-domain files for B. Scope: 3 occurrences across 2 files — `apps/admin/.../listing-edit.component.html` (2 occurrences in inspection empty-state captions) + `mockups/admin/sprint-4-inspection/_shell.html` (1 occurrence in header comment). Replaced with Behbehani-native phrasing matching C's locked pattern. Empty-state captions: "Al Daman integration lands later" → "Score posts after the Behbehani inspector completes the rubric" (when report exists, no score yet) / "Pending Behbehani inspector assignment" (when no report yet). Mockup header comment: "(Al Daman is OUT — ...)" → "no third-party vendor involved". Phrasing factually consistent with existing `project_inspection_internal.md` memory. `nx build admin` GREEN. B-domain repo-wide grep now 0 matches. **Residual** (not B-territory): 5 A-domain mockup files in `mockups/web/sprint-3/` still mention Al Daman (already on A's plate per C's note). STATUS.md historical refs preserved as audit trail. **Drive-by find**: `الضمان` Arabic remnants in apps/web ar.json + apps/mobile ar.json — could be legitimate "warranty" microcopy or missed scrubs; flagged to A and C for sanity check. Posted v1.5.20 ack to CONCIERGE with full before/after table.
- **2026-05-21 B v1.5.19 — Rich-media demo content upgrade (real photos + real CC0 360 turntable)** `[SHIPPED 2026-05-21 B v1.5.19]` — User flagged v1.5.16 synthetic blue-slide MP4s as not-demo-worthy. Replaced both surfaces with real CC0 content. **(a) Per-car walkaround** — 6 personalized 7-sec Ken-Burns MP4s built from each premium listing's Unsplash hero photo via ffmpeg zoompan; customer sees same car on VDP video as listing card; file sizes 354-844 KB. **(b) Real 360 turntable** — downloaded Pixabay video #2708 "Car Red Rotate" (CC0 Pixabay Content License, 2.3 MB at 1280×720); shared across all 6 premium listings; replaces prior cartoon silhouette spin. **(c) Drive-by Patrol fix** — `photo-1606664922998-f180baa4ef91` was 404'd by Unsplash post-v1.3 (broken listing card); swapped to stable `photo-1485463611174-f302f6a5c1c9`. Other 3 broken hero URLs in seed (BMC-SEED-0006/0011/0012) flagged as carry-over. **Files** (8): scripts/generate-rich-media-demo.mjs (rewritten ~190 lines with per-car generator + Unsplash download), 12 new walkaround assets (6 MP4 + 6 poster JPG), 1 new spin360 MP4 (Pixabay), seed.ts (per-listing URL helpers + Patrol photo fix + bytes/frameCount metadata update), smoke probe extended to verify all 13 assets + traversal block, README in demo-media/, .gitignore for source-photos/. **DTO impact: ZERO** — same shapes from v1.5.16; only `url` values change. A's v1.5-D12 frontend components keep working unchanged. **Honest tradeoff**: walkaround is per-car personalized; 360 is one generic red car shared (per-car turntable needs $5k-15k physical rig). Real content drops in via existing admin upload UI when production rig exists. ~6.4 MB net repo growth. `nx build api` GREEN; 13/13 smoke checks PASS. Posted v1.5.19 block to CONCIERGE with full file listing + smoke evidence + operational gates.
- **2026-05-21 B v1.5.18 — `/featured` filter fix (closes A v1.5-D11f [ASK A→B-5])** `[SHIPPED 2026-05-21 B v1.5.18]` — A diagnosed exactly: `GET /v1/public/listings/featured` was ignoring the admin-set `featuredAt` flag, returning "8 most-recent listed sorted inspected-first" so admin's PATCH toggle had zero effect on the customer home rail. Applied A's §3 proposed fix verbatim (~3-line change in `listings-public.controller.ts`): added `featuredAt: { not: null }` filter, switched orderBy to `featuredAt desc`, dropped the in-memory inspected-first sort + 32-row fetch → single-query take 8. Also extended `seedDemoRichMedia()` to set `featuredAt = now() - (i hours)` on the same 6 premium listings already wired for rich media (Porsche / BMW / Nissan / Mercedes / Audi / Lexus). Establishes the v1.5 mental model: **premium tier = featured + rich media**. Rail populates out of the box with Porsche leftmost; admin PATCH toggles override seed timestamps normally. Idempotent re-seed. 2 files, zero migrations, `nx build api` GREEN. Posted v1.5.18 ack to CONCIERGE with 4-scenario behaviour matrix. **B residual**: zero open coordination items.
- **2026-05-21 B v1.5.17 — Production deployment artifacts (nginx + PM2 + env example + DEPLOY.md + deploy script)** `[SHIPPED 2026-05-21 B v1.5.17]` — user asked for EC2 deployment runbook starting with API. Committed 6 tracked files: `infrastructure/nginx/cpo-api.conf` (94 lines — reverse proxy with rate-limit zones for /v1/auth/* + general, security headers, certbot-ready dual-stack config), `infrastructure/nginx/snippets/cpo-proxy-common.conf` (32 lines — shared X-Forwarded-* + timeouts), `ecosystem.config.cjs` (65 lines — PM2 with env_file 5.3+ pattern, source-map support, restart policy), `apps/api/.env.production.example` (115 lines — every env var from `env.ts` annotated with `<CHANGE_ME>` markers + IAM-role-vs-static-keys guidance), `scripts/deploy-api.sh` (137 lines — idempotent: git pull → npm ci → prisma migrate deploy → nx build → pm2 reload → health-probe wait with --skip-migrate / --no-pull / --no-restart flags), `docs/DEPLOY.md` (676 lines — full 3-phase runbook: Phase 0 prerequisites incl. domain registration guidance, Phase 1 EC2/RDS/ElastiCache/S3 provisioning + step-by-step deploy, Phase 2 admin S3+CloudFront, Phase 3 web S3+CloudFront, routine ops + cost summary ~$53/mo + security checklist). Architecture: eu-central-1 Frankfurt, t4g.small EC2 + db.t4g.micro RDS + cache.t4g.micro ElastiCache + S3+CloudFront for SPAs. Deploy script syntax verified clean. All 6 files git-ignore-safe.
- **2026-05-21 B v1.5.16 — Listing rich media surfaced on public VDP (walk-around video + 360° spin) + demo seed** `[SHIPPED 2026-05-21 B v1.5.16]` — closes user directive "for few cars add 360 view and walk around video". Investigation found backend was **100% pre-built but invisible to customers** — `ListingVideo` + `Listing360` Prisma models, admin CRUD (presign/confirm/delete with audit), admin upload UI (`media-gallery.component.ts` + `Media360TabComponent`) all shipped previously; the only gap was `GET /v1/public/listings/:slug` explicitly stripping video/media360 fields per a `// Strips: ... videos, media360` comment in `listings-public.controller.ts:19`. Fixed in-thread (no swarm — sequential work, no race risk). **Changes**: (a) Extended `ListingPublicDetail` with two null-safe fields — `walkaroundVideo: { url, mimeType, posterUrl, durationS } | null` + `spin360: { archiveUrl, mimeType, frameCount } | null`. (b) Extended `DETAIL_INCLUDE` with `videos: { where: { uploadStatus: 'complete' }, take: 1, select: {...} }` and `media360: { select: {...} }`. (c) Extended `toPublicDetail` mapper. (d) New static mount `app.use('/static/demo-media', express.static(...))` with `fallthrough: false`, `immutable: true`, 7-day Cache-Control. Path is intentionally OUTSIDE `/v1/*` namespace. (e) Extended `apps/api/webpack.config.js` `assets` entry to copy `src/seed/demo-media/**` → `dist/seed/demo-media/**` so the same `__dirname`-relative lookup works in dev + prod. (f) New `scripts/generate-rich-media-demo.mjs` — synthesises copyright-clean dummy assets via `ffmpeg-static` + `sharp` (NO internet crawl). Branded color palette + text overlay. Outputs: 6-sec walkaround MP4 (~50 KB) + 28 KB poster JPG + 3-sec 36-frame 360 spin MP4 (~130 KB). Idempotent (`--force` to regen). (g) New `seedDemoRichMedia()` step in `prisma/seed.ts` populates 6 premium listings (Porsche Cayenne, BMW X5, Nissan Patrol, Mercedes C 300, Audi Q5, Lexus RX 350). Idempotent — filters on `s3Key: { startsWith: '/static/demo-media/' }` prefix so real admin uploads are never disturbed on re-seed. (h) New `scripts/smoke-demo-media.mjs` verifies static mount serves 3 known URLs (200 + content-length + content-type) + blocks `../` path traversal (404). All checks PASS. **Deps added** (devDependencies, root): `ffmpeg-static` + `sharp`. Runtime image unchanged. **Files touched** (8): app.ts (path import + static mount), webpack.config.js (assets entry), listings-public.controller.ts (DTO + INCLUDE + mapper), prisma/seed.ts (new function + main() call), NEW scripts/generate-rich-media-demo.mjs (asset gen), NEW scripts/smoke-demo-media.mjs (smoke probe), NEW apps/api/src/seed/demo-media/walkaround/{demo-walkaround.mp4,demo-walkaround-poster.jpg}, NEW apps/api/src/seed/demo-media/spin360/demo-spin360.mp4, NEW apps/api/src/seed/demo-media/README.md. `npx nx build api` GREEN. **Coordination posted**: `[ASK B→A]` in CONCIERGE v1.5.16 — web VDP needs `<video controls playsinline>` for walkaround + Angular `app-spin360-viewer` component for 360 (sample HTML scaffold included; ~1.5–2h estimate). `[ASK B→C]` in MOBILE v0.23-B-reply-3 — react-native-video for walkaround + draggable scrubber 360 viewer (~1h estimate). Both non-blocking — DTO fields are optional/null-safe so older clients ignore them. URL prefix handling documented in both contracts (relative `/static/...` for demo, absolute for production). **Operational gates**: user runs `npm run db:seed` to populate the 6 premium demo rows (no migration needed). B residual: idle, defense-in-depth shipped on listings detail surface.
- **2026-05-21 B v1.5.15 — Listing photo validity guards (closes user directive: no corrupt images on either surface)** `[SHIPPED 2026-05-21 B v1.5.15]` — sonnet swarm (52 calls, 6.2 min wall, all 3 nx builds GREEN + DB audit ran CLEAN lead-verified). **Server-side guards**: (1) Public listings surface filter — every `/v1/public/listings/*` query now includes Prisma `where: { ..., photos: { some: {} } }` so 0-photo listings NEVER reach customer-facing routes (list/featured/low-mileage/detail). Detail route adds explicit 404 `LISTING_NOT_PUBLISHABLE`. (2) Admin publish-stage transition guard in `listings.service.ts.changeStage()` rejects transitions to public stages (`listed | reserved | sold | delivered`) when 0 photos → 422 `LISTING_PHOTOS_REQUIRED`. **DB audit**: new `scripts/check-listing-photos.mjs` standalone Node script scans for (a) 0-photo listings by stage and (b) listings with photos but no `isHero=true` flag. Lead ran against dev DB — **Status CLEAN, 0/0 problematic listings**. **Admin UI img fallback**: 3 components (listing-list table thumb, pipeline-board kanban thumb, media-gallery confirmed-photo) got `(error)="onImgError($event)"` handlers + `data-fallback-applied` infinite-loop guard. On error, swaps `img.src` to inline data-URI SVG (brand-100 background + car silhouette in brand-blues). Local-blob uploads (objectURL) intentionally skipped (can't 404). **Files touched** (8): listings-public.controller.ts + listings.service.ts (API guards), scripts/check-listing-photos.mjs (NEW audit), 3 admin component TS + 2 HTML templates (img fallbacks). **Coordination asks posted**: `[ASK B→A]` to extend the existing `(error)="imageFailed.set(true)"` pattern (already at browse-car-row.component.ts:31) to ~10 more web `<img>` sites (VDP gallery, home featured, saved listings, account, sell wizard, etc.); `[ASK B→C]` to add `onError` to RN `<Image>` components for listing photos + render brand-100 + car-glyph fallback. Both asks framed as defense-in-depth — server filter already prevents the worst case; client handlers cover transient CDN outages + race conditions. Estimated effort A=30 min, C=30 min if batched. **B residual**: still idle; defense layer complete on server + admin surfaces, web + mobile slice depends on A and C wiring their fallbacks.
- **2026-05-21 B v1.5.14 — Tracker DTO rich extension + booking cancel (closes A v1.5-D10 §2+§3 + C v0.23 cancel ASK in single ship)** `[SHIPPED 2026-05-21 B v1.5.14]` — sonnet swarm (55 calls, 7.1 min, `prisma validate + nx build {shared-types,api}` GREEN). Triple-coordination win — A's [ASK A→B-2] + [ASK A→B-3] + C's v0.23 cancel ASK all closed in one cycle. **DTO consolidation**: superseded v1.5.13's `{ name, phoneE164? }` inspector with A's richer `{ fullName, initials, rating?, completedCount?, whatsappE164? }`; legacy fields kept (@deprecated). 4 new fields: `overallScore`, `inspectionReportPdfUrl` (signed S3 GET via new `presignGetUrl` helper, defensive null on S3 fail), `relatedOfferToken`, `cancelledAt`. **toBookingStatus refactored sync→async**; 6 call sites updated. **POST cancel**: `/v1/public/me/sell-bookings/:bookingRef/cancel` mounted on meSellBookingsRouter (A's spec path `/concierge/bookings/:ref/cancel` not added — A to update client OR [ASK A→B-5] alias). Auth + ownership + Zod `{ reason? max 200 }` + idempotent + state-machine guard (only `draft` cancellable). **Inspector notification** via NotificationService.send() + inboxMeta. **Files** (~370 net lines): NEW migration 20260605000005 + 6 edits (schema.prisma, lib/s3.ts, inspection.schemas.ts, inspections.repo.ts, inspections.service.ts, me-sell-bookings.controller.ts). **Future deferred**: rating/completedCount/assignedAt → v1.6+. **Operational gates**: prisma:migrate + nx serve api restart + nx build shared-types + un-comment CancelBookingModal (C) / update BookingsService.cancel$ path (A). Posted v1.5.14 to CONCIERGE + v0.23-B-reply to MOBILE. B residual: still idle.
- **2026-05-21 B v1.5.13 — Sell-bookings me-scoped endpoints + inspector fields (closes both C v0.22 ASKs)** `[SHIPPED 2026-05-21 B v1.5.13]` — investigated C's 2 v0.22 §3 asks, both turned out to be real backend gaps not just doc-deviations. Shipped in-thread (~15 min, no swarm). **(1) sell-bookings-reschedule-endpoint**: B had NO `/v1/public/me/sell-bookings/*` routes at all — mobile's MeSellBookingsApiClient would 404 on every call. Shipped 3 new auth-required endpoints on new `meSellBookingsRouter`: GET list (paginated, concierge-kind-filtered) + GET detail (ownership-checked) + PATCH reschedule. Reschedule validates `preferredDate` (ISO YYYY-MM-DD + custom refine "must be ≥today" UTC compare) + `window` (enum morning/afternoon/evening). Guarded server-side: 404 BOOKING_NOT_FOUND (unknown/not-owned/non-concierge — consolidated to prevent enumeration), 409 BOOKING_NOT_RESCHEDULABLE (status past `draft`). **(2) inspector-fields-on-tracker-dto**: extended `ConciergeBookingStatus` DTO with `inspector: { name, phoneE164? } | null` field, populated from existing inspector relation. Added `mobile` to repo SUMMARY_INCLUDE + DETAIL_INCLUDE inspector select. `toBookingStatus()` updated. Skipped `rating` + `assignedAt` (not modeled today — flagged as v1.6+ future enhancement). **Files touched** (~280 net lines): inspection.schemas.ts (DTO extension + 2 new Zod schemas) + inspections.repo.ts (include extension) + inspections.service.ts (toBookingStatus mapping + 3 new exports listMySellBookings/getMySellBookingByRef/rescheduleMySellBooking) + NEW me-sell-bookings.controller.ts (130 lines, 3 routes + auth + InspectionError adapter) + app.ts (import + mount before meAccountRouter). No new migration. `nx build {shared-types, api}` GREEN. **Operational gates for C**: `nx serve api` restart + optional smoke `curl -H "Authorization: Bearer <jwt>" .../v1/public/me/sell-bookings → { items: [], total: 0 }`. Mobile's existing client should match this shape since it was the spec target. Posted v1.5.13 block to MOBILE with full endpoint contracts + DTO additions + 6-row error table + operational verify table. B residual: still idle, no new asks introduced.
- **2026-05-21 B v1.5.12 — Admin UI gaps closed (Maintenance queue + Feature Waitlist)** `[SHIPPED 2026-05-21 B v1.5.12]` — proactive audit found 2 admin UIs missing despite B endpoints existing → both shipped via sequential swarm chain (~15 min total wall-clock, all 3 nx builds GREEN lead-verified after each swarm). **(a) v1.5.12.a Maintenance Requests admin queue** (sonnet, 50 tool calls, 8.5 min) — closes operational dead-end where customer maintenance requests submitted via mobile (C v0.19.b) had no admin UI to action. New `/operations/maintenance` (list w/ status filter chips + statusCounts + search debounce) + `/operations/maintenance/:id` (detail w/ reactive status-transition form + standalone admin-notes form). 3 new B endpoints `/v1/admin/maintenance-requests/*` (GET list, GET detail, PATCH). **State machine** enforced server-side: pending_review → {scheduled, cancelled}; scheduled → {in_progress, cancelled}; in_progress → {completed, cancelled}; completed/cancelled terminal. Illegal transitions → 409 `MAINTENANCE_INVALID_STATUS_TRANSITION`. **Validation per user emphasis**: server Zod 2× `.refine()` (scheduled requires scheduledFor + adminNotes≥5; cancelled requires cancellationReason≥5) + state-machine guard; client Angular Reactive Forms with dynamic Validators (re-applied when status dropdown changes) + custom `futureDateValidator` (no past dates) + Save disabled until form valid+dirty+!busy. New migration `20260605000004_v1_5_12a_maintenance_admin_fields` adds `cancellationReason` column. Nav placeholder `<span aria-disabled="true">` replaced with active `<a routerLink="/operations/maintenance">` under widened role gate `[operations_manager, maintenance_coordinator, general_manager, super_admin]`. **(b) v1.5.12.b Feature Waitlist admin** (sonnet, 35 tool calls, 6.6 min, no migration needed) — operations team can now see who's on the concierge/financing/returns/etc. waitlists. New `/operations/feature-waitlists` page w/ feature filter chips + pathCounts + search (300ms debounce + distinctUntilChanged) + paginated table + CSV export button. 2 new B endpoints: GET list + GET export (`text/csv; charset=utf-8` attachment, 10k row cap with `X-Export-Cap-Hit` header). **CSV safety**: every cell passes through `csvCell()` — OWASP CSV injection prevention (cells starting with `=`/`+`/`-`/`@` are prefixed with `'`) + RFC 4180 quote-wrap on commas/quotes/CR/LF. Role gate `[operations_manager, general_manager, super_admin]`. Nav entry added under Operations group. **Net delivery**: 11 files created + 10 files edited across both swarms; 5 new admin endpoints; 1 migration; 0 stakeholder-parked items touched. All 3 `nx build {shared-types, api, admin}` GREEN, `prisma validate` VALID. **B residual**: only the v1.5 admin UI gaps remain that depend on parked items (Payment Reconciliation needs Otto creds; Documents global queue needs v1.6 Loan/Dealer modules). Backlog now truly empty.
- **2026-05-21 B v1.5.11 — reviews-listing auth fix (closes C v0.20 ASK)** `[SHIPPED 2026-05-21 B v1.5.11]` — C's §13.5 smoke probe in v0.20 caught a real spec deviation: `GET /v1/public/listings/:id/reviews` returned 401 AUTH_REQUIRED despite B v1.5.6 §3 explicitly spec'ing it as no-auth. C correctly diagnosed it as a route-middleware config issue + proposed Option A (recommended). **Root cause was NOT a router-config bug** — `reviewListingsPublicRouter` was correctly built without `requireCustomerSession` middleware. The 401 was an **Express mount-order collision**: `meAccountRouter` mounted at the broad `/v1/public` prefix on line 81 of app.ts applies `requireCustomerSession` via `router.use(...)` which intercepts ALL `/v1/public/*` requests it sees. `reviewListingsPublicRouter` was mounted at line 91 AFTER meAccountRouter → never reached. **Fix**: moved the mount to immediately after `listingsPublicRouter` (line 70) so it precedes the catch-all auth. Added a `// v1.5.11` comment block explaining the constraint for future router additions. `nx build api` GREEN. **Memory saved**: `feedback_express_middleware_order_trap.md` — rule for future B work: any new no-auth `/v1/public/*` router MUST mount BEFORE meAccountRouter; future swarm prompts to include this constraint. **Operational gate for C**: `nx serve api` restart + re-run curl probe from v0.20 §3 row 2 → expect 200 + empty ReviewListResponse DTO. No mobile code change needed. Posted v1.5.11 block to MOBILE with diagnosis + fix + convention note. B residual: still idle, 14/14 v1.5.6 endpoints now spec-compliant.
- **2026-05-21 B v1.5.10 — Avatar upload presigned-URL endpoint (closes A v1.5-D7 TODO)** `[SHIPPED 2026-05-21 B v1.5.10]` — A's profile.component.ts:141 TODO comment "v1.5-D7: avatar upload endpoint not yet on B side" caught during routine status check + shipped in-thread (~30 min, no swarm). New endpoint **POST `/v1/public/me/avatar/upload-url`** returns `{ url, key, expiresAt }` via existing `presignPutUrl` helper. 3-step flow mirrors admin Documents v1.4.4: client POSTs → S3 PUT → PATCH `/me/profile` with returned key. Validation: MIME enum (jpeg/png/webp only — HEIC/HEIF/GIF rejected to avoid server-side conversion), size cap via new `env.MAX_AVATAR_BYTES` (default 5 MB). Server-set key shape `avatars/<userId>/<uuid>.<ext>` — collision-free + re-upload-safe. Rate limit `sensitiveActionLimiter` (5/min/user) defends against presigned-URL exhaustion. **5 files touched, ~115 net lines**: env.ts (1 var), me-account.schemas.ts (3 Zod schemas + 2 new error codes in `ME_ACCOUNT_ERROR_CODES`: `AVATAR_TOO_LARGE`, `AVATAR_MIME_NOT_ALLOWED`), me-account.service.ts (`presignAvatarUploadUrl()` export + imports for randomUUID/presignPutUrl/env + HTTP status map extension), me-account.controller.ts (new POST route). No new migration, no new shared infra (reuses existing helpers). `nx build {shared-types,api}` GREEN. **Operational gates for A**: `nx serve api` restart + `nx build shared-types` rebuild + wire profile.component.ts upload button (file picker → upload-url POST → S3 PUT → PATCH profile) + remove TODO comment. **C-side note**: mobile profile.tsx still initials-only; same 3-step flow works on mobile via expo-image-picker — unblocked but optional. No `[ASK B→C]` introduced. Posted v1.5.10 block to CONCIERGE with full endpoint contract + 5-row error table + 3-step flow + Janitor-cron-deferred-to-v1.6 note. B residual: still idle.
- **2026-05-21 B v1.5.9 — BullMQ socket leak closed (#66 follow-up)** `[SHIPPED 2026-05-21 B v1.5.9]` — closed the cosmetic teardown leak surfaced by v1.5.8's test infra ship. Root cause: `inspections.service.spec.ts` transitively imports `pdf-worker.ts` which instantiates a BullMQ `Queue` against real `redisClient()` at module-load time → real ioredis socket opens, never closes, jest emits "A worker process has failed to exit gracefully." Fix: added `jest.mock('../jobs/pdf-worker', () => ({ enqueueInspectionReportPdf: jest.fn().mockResolvedValue(undefined) }))` at top of spec — the signoff flow only fires-and-forgets the dispatch so a no-op mock is fully sufficient. `nx test api`: **12 suites + 202 tests PASS, zero teardown warnings**. ~5 min wall-clock, main-thread, no swarm. B backlog now empty (no pending tasks except user-gated items: Otto / PACI / APNs / Apple).
- **2026-05-21 B v1.5.8 — both B-backlog items closed (#24 + #38)** `[SHIPPED 2026-05-21 B v1.5.8]` — kicked off the last two long-pending B-backlog tasks; both landed clean. **#24 Test infra (Redis mock + jest env init)** — sonnet swarm (64 tool calls, 16.9 min wall). Created `apps/api/src/test/setup-env.ts` (jest setupFiles entry; defaults all env vars before module import per §13.5 — no real .env dependency) + `apps/api/src/test/mocks/redis.mock.ts` (InMemoryRedis covering complete ioredis surface used by API: get / set with EX+NX / del / incr / expire / flushdb + test-only `_peek()` / `_setExpired()` / `_getRaw()` inspection helpers) + edited `apps/api/jest.config.cts` (setupFiles + testTimeout:10000). Smoke test fixed: `apps/api/src/auth/otp.service.spec.ts` was previously failing with `TypeError: Cannot read properties of undefined` (notification transitive import + prisma auto-mock failure); rewrote with factory mocks for prisma/bcrypt/otp-notifications. **Result: 12 test suites + 202 tests all PASS** (`nx test api` GREEN, lead-verified). One pre-existing cosmetic teardown leak surfaced (`inspections.service.spec.ts` imports pdf-worker which opens a real BullMQ Redis socket — doesn't fail any test, just emits "worker has failed to exit gracefully" warning). Tracked as follow-up task #66, ~5 min fix when next free cycle. **#38 react-pdf toBuffer runtime verify** — built `scripts/check-react-pdf-tobuffer.mjs` standalone probe, ran in main thread (~30s). **FOUND A REAL BUG**: `pdf().toBuffer()` returns a Readable PDFDocument stream NOT a Buffer (`Buffer.isBuffer(result) === false`, `result.length === undefined`). Downstream consequence: `admin-order.service.ts:198` reads `contractBytes.length` as `fileSizeBytes` for the Document row insert — every shipped sale_contract PDF would have had NULL/undefined fileSizeBytes silently. **Fix applied** to `sale-contract-pdf.service.ts:147` — now properly collects the stream via `for await (chunk of stream) → Buffer.concat(chunks)`. Returns real Buffer with valid `.length` (verified 1582 bytes matches on-disk fs.stat). `nx build api` GREEN. Probe script kept in-repo as regression check (re-run after any `@react-pdf/renderer` version bump). **Memory saved**: `feedback_verify_library_return_shapes.md` — rule for future B work: when casting library returns to non-obvious types (e.g. `as unknown as Buffer`), write a probe script before trusting the cast. B residual: pending #66 (BullMQ socket leak follow-up — cosmetic only, non-blocking). No new asks introduced.
- **2026-05-21 C v0.19 — 3-agent parallel wire to B v1.5.6** `[SHIPPED 2026-05-21 C v0.19]` — same-cycle close-out: B shipped all 3 C-asks (notifications + maintenance + reviews backends, 14 endpoints + 3 migrations) and C immediately spawned 3 sonnet agents in parallel to wire the mobile surfaces. All 3 landed clean. **v0.19.a Notifications** — new `MeNotificationsApiClient` (5 methods: list/getUnreadCount/markRead/markAllRead/delete) + `/notifications` route (6-state infinite-scroll + sticky "Mark all read" CTA + optimistic mutations) + 4 sub-components (NotificationListItem with channel glyph + CategoryBadge + DeleteConfirmModal + states) + `useUnreadNotificationsCount` hook in `apps/mobile/src/hooks/` (30s staleTime, 60s refetchInterval) + account-hub tile fixed with `CountPill` (0 hidden, >99 → "99+"). **CRITICAL deviation honored**: server pre-localizes title/body per User.locale — mobile renders directly without `t()` wrap; only labels around them are translated. 26 `notifications.*` keys. **v0.19.b Maintenance** — `MeMaintenanceApiClient` (5 methods + Idempotency-Key on create) + 3 routes (`/maintenance` list with All/Open/Closed filter chips + `/maintenance/[id]` detail with 409 `MAINTENANCE_REQUEST_NOT_CANCELLABLE` cancel-race handler + `/maintenance/new` modal create form with 14-day date strip + chip pickers for governorate/window/category + 500-char notes counter) + 5 sub-components (MaintenanceStatusPill brand-only, MaintenanceListItem, MaintenanceListStates, MaintenanceForm, CancelConfirmModal). **CRITICAL deviation honored**: governorate wire value is snake_case `mubarak_al_kabeer` NOT `mubarakAlKabeer` per B's existing Prisma enum; mobile DUPLICATES governorate keys with snake_case to keep wire↔display binding clean (doesn't reuse sell-flow's camelCase keys). ~54 `maintenance.*` keys incl. snake_case governorate sub-keys. **v0.19.c Reviews** — **2-client split** preserving B's auth boundary: `MeReviewsApiClient` (httpClient/auth — list+create+delete) + `ListingReviewsApiClient` (rawHttpClient/no-auth — public listForListing for VDP) + `/reviews` route (my-reviews infinite-scroll list with delete) + VDP integration via extracted `ReviewsSection` component (preserves `listings/[slug].tsx` at ~382 lines, well under 500) + 8 sub-components incl. `StarRatingInput` (5 tappable stars, brand[700] filled / slate[300] empty — **NO yellow per brand-lock**, ARIA radiogroup, ≥44px touch) + `WriteReviewModal` (star + title 80c + body 1000c counter + Idempotency-Key per modal open + inline 403 `REVIEW_TARGET_NOT_REVIEWABLE` + 409 `REVIEW_ALREADY_SUBMITTED` error mapping) + `ReviewsAggregate` (large average + 5-bar histogram + total count, brand[700] bars). ~40 `reviews.*` keys. **Account-hub stub-closer continuation**: 3 more tiles fixed (Notifications/Maintenance/Reviews — pill removed + onPress repointed) — **only 2 Coming-Soon tiles remain by design**: Financing (Q4-2026, gated on SRS §3.7 bank-statement-upload partner integration) + Referrals (2027, referral-code generation infra not in v1.5 scope). 8 of 10 account-hub tiles now wired to real routes. **Final mobile state**: locales **EN 927 / AR 927 symmetric across 24 namespaces** (+ maintenance, notifications, reviews this drop = locales 14.5x v1.0 baseline's 64 keys); `npx tsc --noEmit -p apps/mobile/tsconfig.json` exits 0; `npm run guard:i18n-parity-mobile` exits 0 with "Missing: 0 Extra: 0"; `npm run guard:brand-lock-mobile` exits 0 across **189 mobile files** (+25 since v0.18). **§13.5 user gate for smoke testing**: `npm run prisma:migrate` to apply B's 3 new migrations (`20260605000001_notifications_inbox` + `20260605000002_maintenance_requests` + `20260605000003_reviews`) + restart `nx serve api` to load the 14 new routes. Mobile code is migration-agnostic — compiles cleanly today. All 3 prior `[ASK C→B]` closed in this cycle. Mobile session fully idle pending user §13.5 ops or new direction.

- **2026-05-21 B v1.5.7 — Inspection-report endpoint shipped (closes A v1.5-D §5)** `[SHIPPED 2026-05-21 B v1.5.7]` — A's pending `[ASK A→B]` from v1.5-D §5 was caught during a routine status check and shipped in-thread (~10 min wall, no swarm). Endpoint: `GET /v1/public/concierge/offers/:token/inspection-report` → returns `PublicInspectionSummary` (reuses existing shared-types shape from `/inspection-sign/:token`). Error mapping per A spec: 410 `OFFER_LINK_EXPIRED` (token expired / offer withdrawn), 404 `INSPECTION_NOT_AVAILABLE` (token missing / inspection not signed_off / inspection row absent). **3 files touched, ~75 net lines**: (a) `apps/api/src/inspections/inspections.service.ts` — new public-shared `getInspectionReportById(id)` export (validates signed_off + returns toPublicSummary); (b) `apps/api/src/offers/offers.service.ts` — new public-shared `getInspectionReportByOfferToken(token)` export (validates offer expiry + non-withdrawn, delegates to inspections.service, translates InspectionError → OfferError for the controller's error adapter); (c) `apps/api/src/offers/offers-public.controller.ts` — new GET route mounted on existing `offersPublicRouter` with same `publicReadLimiter` (60 req/min). No new shared-types, no new migration, no new auth. `nx build api` GREEN. **C operational gates for A per §13.5**: just `nx serve api` restart needed to load the new route — A's `/offer/:token/inspection-report` CTA goes live immediately. Posted v1.5.7 block to CONCIERGE with full endpoint contract, 5-row error mapping table, file-diff summary, build verify status, and edge case docs (inspection un-signed scenario — A's existing "Report not available" state handles gracefully). B residual: still idle, no new asks introduced.
- **2026-05-21 B v1.5.6 — all 3 C-asks shipped (notifications + maintenance + reviews)** `[SHIPPED 2026-05-21 B v1.5.6]` — closed all 3 `[ASK C→B]` from v0.17/v0.18 via **sequential 3-swarm chain** (sonnet × 3, ~16 min total wall-clock, schema.prisma race-collisions avoided). Lead-verified `prisma validate` + both `nx build {shared-types,api}` GREEN after each swarm landed (swarm 2 sandbox was Bash-blocked; lead ran verify post-spawn — exactly the §13.5 / `feedback_build_pass_not_shipped.md` discipline). **14 new endpoints LIVE** across 3 surfaces: **(1) v1.5.6.a notifications-inbox** — Notification Prisma model + 3 enums (NotificationChannel/Category/IconHint) + migration `20260605000001_v1_5_notifications_inbox` + Zod schemas + 5 endpoints (list/unread-count/read/read-all/delete) + **NotificationService persistence extension** (new `inboxMeta` param on `send()`, new `sendInApp()` export, best-effort try/catch so DB row insert never breaks an FCM dispatch). DTO uses server-side localization — `title`/`body` resolved per `User.locale` via per-request DB lookup, mobile receives single strings not titleEn/Ar split. **(2) v1.5.6.b maintenance-pickup-capture** — MaintenanceRequest model + migration `20260605000002_v1_5_maintenance_requests` + Zod schemas + 5 endpoints (list/get/create-with-Idempotency-Key/patch/delete) + 3 error codes. Reused existing `KuwaitGovernorate` enum (note: DB value is snake_case `mubarak_al_kabeer` not `mubarakAlKabeer` per spec sketch) + existing `PreferredWindow` enum (would have been P3018 collision otherwise). Idempotency-Key column added matching Orders v1.4 pattern. Status query supports `?status=open|closed` (open = {pending_review/scheduled/in_progress}; closed = {completed/cancelled}). **(3) v1.5.6.c reviews-capture** — Review model with polymorphic target (listing OR service) via discriminator + 2 enums + migration `20260605000003_v1_5_reviews` + Zod schemas + 4 endpoints (1 PUBLIC `/v1/public/listings/:id/reviews` no-auth + 3 auth-required `/v1/public/me/reviews/*`) + 3 error codes. **Reviewability guard** checks customer has completed Order (listing target) / signed_off InspectionReport (inspection service) / completed MaintenanceRequest (maintenance service) BEFORE accepting POST. Compound uniques prevent duplicates. Server-side `anonymizeName()` returns "First L." display name; userId NOT exposed in DTO. Router split into two named routers (`reviewMePublicRouter` auth + `reviewListingsPublicRouter` no-auth) for Express config clarity. DB CHECK constraint `rating >= 1 AND rating <= 5`. **C operational gates per §13.5**: user runs `npm run prisma:migrate` to apply 3 new migrations + `nx serve api` to load 14 new routes + `nx build shared-types` for the new DTOs + optional curl smoke `GET /v1/public/me/notifications/unread-count → { count: 0 }`. Posted v1.5.6 consolidated reply block to MOBILE_API_CONTRACT.md with full endpoint tables / DTO shapes / migration filenames / reviewability guard SQL / 3 error-code tables / mobile wiring suggestions. B residual: still idle, no new asks introduced.
- **2026-05-21 C v0.18 — offer wire-up + 3-stub closer + 2 new ASKs** `[SHIPPED 2026-05-21 C v0.18]` — **(a) Offer flow wire-up** (opus, ~12 min wall-clock, 70 tool calls). New `OffersPublicApiClient` in `libs/data-access-mobile/src/lib/offers-public.client.ts` wraps **rawHttpClient** (shared-link no-auth invariant preserved — same posture as inspection-sign deep link); `getByToken` (Zod-parsed via `PublicOfferViewSchema`) + `respond('accept'\|'decline'\|'counter', payload?)` with local Zod schema at boundary. 4 of 5 offer state screens wired to real `useQuery(['offer', token], …)` — view (22 t() + defensive `hasInspection` check + CTAs disabled when `!offer.canRespond`), counter (22 t() + `useMutation` for respond with KWD→fils parse + D1 LOCK preserved zero "1 round" copy), accepted (18 t() in hero/steps/ref-card), expired (9 t(), retry:false to tolerate 410). `declined.tsx` intentionally fetch-free — pure terminal "thanks + WhatsApp" page; fetching would race the server token retirement. URL prefix is `/v1/public/concierge/offers/:token` (matches A's web `OffersService` + API controller). 80 total t() calls. **CLOSES v0.16 carry-over** — view.tsx inspection card now `router.push(\`/inspections/${offer.inspectionReportId}\`)` per B v1.5.4's new field; `grep test-inspection-id apps/mobile` → 0 hits. 3 new `offers.view.{loading,error,retry}` keys + `offers.counter.amountHint` reworked to `{{amount}}` interpolation. **(b) Profile + Addresses + Favorites stub closer** (sonnet, ~15 min wall-clock, 96 tool calls). Two new clients: `MeAccountApiClient` (6 methods: getProfile / updateProfile / listAddresses / createAddress (Idempotency-Key) / updateAddress / deleteAddress) + `MeSavedListingsApiClient` (list / add (Idempotency-Key) / remove). Three new routes: `/profile` (455 lines — full name + email/mobile with Verified badges + locale toggle + Save CTA disabled-until-dirty), `/addresses` (332-line orchestrator + AddressFormModal 308 + DeleteAddressModal 120 with 6-KW-governorate chip picker reused from sell-flow), `/favorites` (496 lines — infinite-scroll FlatList + skeleton/empty/error/list states + optimistic remove via useMutation + filled heart + KWD 3-decimal). Three account-hub tile fixes (Profile + Addresses off `/auth/sign-in` stubs; Favourites off `/(tabs)/browse` workaround → real `/favorites`). Three new i18n namespaces: `profile.*` 17 + `addresses.*` 45 (incl. 6 governorate keys) + `favorites.*` 12 = **74 new keys EN+AR symmetric**. All Stack.Screen entries registered. All new files < 500 lines. **(c) Two new `[ASK C→B]` posted** to MOBILE_API_CONTRACT v0.18 §1 + §2: **maintenance-pickup-capture** (5 endpoints GET list/GET id/POST/PATCH/DELETE; full DTO with 6 KW governorates reused from sell-flow + concern category enum + status state machine + adminNotes; 3 error codes incl. `MAINTENANCE_REQUEST_NOT_CANCELLABLE` 409; Idempotency-Key on POST), and **reviews-capture** (4 endpoints incl. PUBLIC `/v1/public/listings/:id/reviews` for VDP + auth `/v1/public/me/reviews`; polymorphic target via discriminated union; reviewability check per Order; 3 error codes incl. `REVIEW_ALREADY_SUBMITTED` 409). **Returns explicitly NOT asked** this round — order-flow-tied, requires completed Order, blocked on Otto until unblock. **Final state**: locales **EN 810 / AR 810 across 21 namespaces** (+ `addresses`, `favorites`, `profile`); `npx tsc --noEmit -p apps/mobile/tsconfig.json` exits 0; `guard:brand-lock-mobile` 0 violations across 164 mobile files; `guard:i18n-parity-mobile` exits 0. **3 asks now pending with B** (notifications-inbox from v0.17 + maintenance + reviews). Per B's v1.5.3 precedent (4.9 min wall-clock for Saved Searches backend), all 3 could plausibly land same-day if B parallelizes.
- **2026-05-21 A v1.5-D6 — /account/* settings-shell rebuild (sidebar nav + content pane)** `[SHIPPED 2026-05-21 A v1.5-D6]` — User feedback: card-based profile didn't feel like a settings app + header avatar dropdown had duplicate "Profile" + "Account" items. Decision: build a unified shell with persistent left sidebar (desktop) + horizontal pill scroll (mobile), retire the tile-grid hub, strip hero+back-link from all account sub-pages, route everything under one parent route. **In-thread by lead (3 min)**: removed "Profile" item from header avatar dropdown in `apps/web/src/app/layout/shell.component.ts` — dropdown is now Account → My bookings → Saved cars → Sign out. **Ruflo agent (opus, ~18 min, 106 tool calls)**: created `AccountLayoutComponent` (158 lines) + `shell/account-nav.ts` shared nav data (112) + `shell/sidebar-desktop.component.ts` (98) + `shell/sidebar-mobile-pills.component.ts` (60). **Routes restructure**: `/account` converted from leaf route (the hub) to parent route loading `AccountLayoutComponent` with `children: [...]` of 14 nested children (profile/addresses/notifications/security/documents/orders/orders/:id/saved-searches/inspections/favorites + 5 coming-soon: maintenance/financing/returns/reviews/referrals) + `{ path: '', pathMatch: 'full', redirectTo: 'profile' }` default. Legacy top-level redirects preserve old URLs: `/my-bookings` → `/account/inspections`, `/my-bookings/saved-cars` → `/account/favorites` (zero breakage for push deep-links, email links, bookmarks). **Stripped hero + back-link** from all 12 child pages (class logic — signals, computed, validators, save handlers, polling, countdown, cancel-reservation flow, formatDate/formatDateTime — ALL preserved): profile / addresses / notifications / security / documents-page / orders-page / order-detail-page (gradient hero replaced with compact header but status pill + countdown + cancel UNTOUCHED) / saved-searches-page / my-bookings / saved-listings (also removed in-hero sub-nav between bookings↔saved-cars since sidebar handles that) / coming-soon-page (ETA pill now bg-brand-50/text-brand-700 instead of white-on-blue). **DELETED** `account-hub.component.ts` — tile-grid hub retired. **Sidebar UX**: desktop fixed 280px sticky sidebar inside `container-page max-w-7xl` with `md:grid-cols-[280px_1fr]`, 4 groups (PROFILE & SETTINGS / BUYING / OWNING / ENGAGEMENT matching prior hub structure), active state `bg-brand-50 text-brand-700 font-semibold` (NEVER emerald), coming-soon items get neutral `SOON` badge `bg-surface-cool text-muted` (NEVER amber/yellow), sign-out at bottom (`text-red-600 border-red-200`, red allowed for destructive). Mobile (<md): top horizontal scrolling pill row with flattened 14 items, active pill `bg-brand-700 text-white border-brand-700`, group labels hidden on mobile. RTL via Tailwind logical properties (me-auto, border-e, ms-2, start/end). **i18n**: +26 keys symmetric — `account.shell.nav.*` (17: ariaLabel + 14 item names + signOut + comingSoon) + `account.shell.page.*` (9 title/sub pairs for profile/addresses/notifications/security/documents/orders/savedCars/savedSearches/inspections). Reuses existing `account.hub.groups.*` for group labels and `account.hub.signInRequired.*` for guest gate. **Verification matrix**: `nx build web --skip-nx-cache` PASS (20.9s, no warnings), `npm run guard:brand-lock` PASS, `npm run guard:i18n-parity` PASS, **browser-verified live** (Chrome MCP desktop): /account redirects to /account/profile ✅, sidebar shows 4 groups + 14 items + sign-out with Profile active in brand-50 ✅, right pane "Profile" header + Your details / Email / Mobile / Password cards (verified pills brand-blue NOT emerald) ✅, **cross-route click → Addresses persists sidebar with zero remount** ✅, avatar dropdown clean (no duplicate Profile) ✅. **Coordination**: agent kept ABSOLUTE routerLink refs throughout, so all cross-codebase references (sign-in modal navigates to /account → redirects to /account/profile; sign-up modal navigates to /my-bookings → redirects to /account/inspections; existing dropdown links continue working) need ZERO edits outside `apps/web/src/app/features/account`. **Open follow-ups (low priority)**: (a) mobile pill row could use fade-edge indicator for "more pills offscreen" hint, (b) `profile.component.ts` (823) + `notifications.component.ts` (530) exceed 500-line cap (pre-existed; not caused by this task — refactor when convenient), (c) full RTL visual walk by user when on AR side. v1.5-D6 contract block posted to CONCIERGE.
- **2026-05-21 A v1.5-D5 — /sell/concierge wizard + tracker FULL v2 mockup rebuild** `[SHIPPED 2026-05-21 A v1.5-D5]` — User flagged that v1.5-D4 only canonicalized the hero — the BODY of `/sell/concierge` + entire `/sell/concierge/status/:ref` tracker were still v1 designs. Approved v2 mockup at `mockups/sprint-4-redesign/sell-concierge-v2.html` (765 lines) had been sitting un-shipped since sprint-4. v1.5-D5 closed the gap end-to-end via 2 parallel ruflo agents (opus, both run_in_background). **Wizard agent** (~17.7 min, 60 tool calls): rewrote `concierge-page.component.ts` (582→494 lines) + created 4 step/success sub-components + 2 new shared components (`address-autocomplete.component.ts` typeahead with Google Places adapter pattern + `date-strip.component.ts` 14-day horizontal scroll) + `address-suggestion.service.ts` adapter (auto-switches mock KW seed → real Places API when `window.google?.maps?.places` is detected). **Tracker agent** (~12.6 min, 44 tool calls): rewrote `concierge-status-page.component.ts` (403→499 lines) + 2 new sub-components (`tracker-timeline.component.ts` 183 lines + `tracker-inspector-card.component.ts` 88 lines). Both agents preserved ALL existing class logic (signals, computed, ngOnInit guards, polling, route params, validation, toDto, submit, formatDate/formatDateTime) — only templates + minor new state signals changed. **In-thread integration fixes**: (a) tracker `reportLink()` was wired to non-existent route `/sell/inspection-report/:bookingRef` — lead changed to return null, template `@if (reportLink(); as link) { <a> } @else { <button disabled> }`, new EN+AR i18n key `viewReportPending` = "Report available with your offer"; lights up automatically when B extends DTO (see Ask A→B-2). (b) self-introduced parse error: my fix-up template comment contained backticks that terminated the outer template-literal — replaced with bare text. **Mockup-fidelity wizard** (live-verified via Chrome MCP walk /sell/details → /sell/choose → "Sell it for me" → /sell/concierge): ✅ hero trust strip (3 brand-blue chips, NOT emerald — "Completely free" / "71-point inspection at your door" / "Guaranteed cash offer in 24h"), ✅ Step 1 two-card layout (Address card with typeahead autocomplete + collapsed `<details>` parking notes + Schedule card with horizontal day-strip + 3-up time-window cards with SVG icons + reassurance banner), ✅ Step 2 privacy trust micro-strip (brand-50, brand-700 shield — translated from mockup's emerald-50/600), ✅ Step 3 promise block + 4 review cards (Vehicle/Location/Schedule/Contact with circular icon chips + Edit link). **Mockup-fidelity tracker** (visual self-check ✅ all 7): hero bookingRef anchor chip + copy button (in-progress) / no copy (signed), 4-step timeline with brand-700 done circles + brand-700 active ring + opacity-60 pending (NOT emerald per v1.5-D1 lesson), inspector card with gradient avatar + brand-700 star (NOT amber-500) + 2-button row (WhatsApp = brand-700 NOT emerald-500 + Call), 3-up quick actions (Add to calendar ICS blob client-side SSR-safe / Reschedule stub / Cancel red-600 destructive with tel: fallback), signed-state hero kept brand-blue gradient with white/20 "Signed & complete" chip + different H1/sub, score card with 40px brand-700 score (NOT emerald-700) + INSPECTED BY column + 2 CTAs, brand trust footer. **Brand-lock translations**: emerald-300/500/600/700 → brand-700/900/text-white, emerald-50 → brand-50, amber-500 → brand-700, amber-50/700 → slot badges REMOVED entirely (no backend data). **i18n**: 75 new keys across `sell.concierge.hero.*` (3), `sell.concierge.address.*` (~9), `sell.concierge.location.*` (6 extensions), `sell.concierge.contact.*` (4), `sell.concierge.reviewStep.*` (7), `sell.conciergeTracker.*` (47), all EN+AR symmetric with formal customer-register Arabic. Tightened existing copy (`sell.concierge.steps.location` "Location & when" → "Where + When", `location.windowMorning/Afternoon/Evening` dropped redundant hours that now render in card subtitle, `reviewStep.submit` → "Confirm my booking"). **Verification matrix**: nx build web PASS (15-33s across iterations), guard:brand-lock PASS (post-translation sweep — zero violations), guard:brand-lock-mobile PASS (189 files, C unaffected), guard:i18n-parity PASS (EN/AR synced), guard:i18n-parity-mobile PASS (927/927, C unchanged), guard:secrets PASS. **3 new asks/gates posted** to CONCIERGE_INSPECTION_API_CONTRACT.md v1.5-D5 §5+§6: `[ASK A→B-2]` extend ConciergeBookingStatusSchema with inspector+score+reportPdfUrl+relatedOfferToken (tracker stubs flip live when landed), `[ASK A→B-3]` POST /v1/public/concierge/bookings/:ref/cancel endpoint, `[GATE]` GOOGLE_MAPS_API_KEY provisioning (Places + Maps JS APIs, restrict-by-referrer, drop into apps/web env config). **Pre-existing initial-bundle warning unchanged** (973 kB vs 500 kB cap — predates v1.5-D5, tracked as A's bundle-budget cleanup pick).
- **2026-05-21 A v1.5-D4 — Sell concierge page hero canonicalization** `[SHIPPED 2026-05-21 A v1.5-D4]` — Follow-up to v1.5-D after user flagged `/sell/concierge` UI was still pre-canonical. Page was using the legacy full-bleed `<header class="border-b border-line bg-gradient-to-br from-brand-900 via-brand-700 to-brand-600">` pattern with stepper inactive pills nearly invisible against gradient (`bg-white/10`). **Converted to canonical pattern** matching `/account/profile`, `/account/addresses`, `/offer/:token`: (1) back-link extracted to its own `container-page pt-6 > mx-auto max-w-4xl > <a class="text-brand-700 hover:underline">` block above hero, (2) hero is now `container-page py-6 mx-auto max-w-4xl > rounded-3xl p-6 sm:p-8 text-white` with `style="background: linear-gradient(135deg, #1E3A8A 0%, #1D4ED8 60%, #2563EB 100%)"` inline, (3) "Concierge Service" badge moved inside hero card as a top chip, (4) stepper kept inside hero but inactive pills now `bg-white/20 border-white/30 text-white` with inner number-bubble `bg-white/30` — legible against blue gradient instead of merging with it (active pill kept as white card + shadow-brand-sm + brand-700 text), (5) main body width matched to `container-page py-8 sm:py-12 max-w-4xl mx-auto`. No i18n changes — reuses existing `sell.concierge.{back,badge,title,sub,stepCount,stepsNav,steps.*}` keys. `nx build web` PASS (18.4s, no new chunks). `npm run guard:brand-lock` PASS. **Browser-verified live**: walked /sell/details → /sell/choose → "Sell it for me" → /sell/concierge; hero renders as centered rounded blue card matching account-page canon, stepper inactive pills now clearly visible. | — | `apps/web/src/app/features/sell/concierge-page.component.ts` |
- **2026-05-21 A v1.5-D — Sell-flow close-out** `[SHIPPED 2026-05-21 A v1.5-D]` — 3 small + 1 medium pickups bundled to close the sell-car customer flow. **(a) v1.5-D1 emerald gradient FIX on `offer-page.component.ts`** — accepted-state hero had `[class.from-emerald-700]` / `via-emerald-600` / `to-emerald-500` swap (lines 70-72) that slipped past brand-lock guard because guard only scanned `bg-/text-/border-` prefixes. Hero now stays brand-blue for ALL offer states; accepted-state differentiation moved to inner checkmark icon + copy. **(b) v1.5-D1b guard extension** — `scripts/guard-brand-lock.mjs` BANNED_PREFIXES extended from `['bg','text','border']` → `['bg','text','border','from','via','to']` so any future `from-emerald-*` / `via-amber-*` / `to-green-*` etc. fails the gate. Code comment documents v1.5-D rationale. **(c) v1.5-D2 brand-logo `(error)` fallbacks** in 5 component files — when favicon URLs 404 (common — half our brand seeds are bare-letter clearbit favicons), `<img>` previously rendered broken-image icon. Added SSR-safe `onLogoError` / `onIconError` handler that hides `<img>` and injects circular `bg-brand-100 text-brand-700` initial-letter chip in the same parent span: `browse-filter-panel` (1 site, `w-3.5 h-3.5 text-[8px]`), `home/sections/browse-by-brand` (2 branches, `w-8 h-8 sm:w-10 sm:h-10`), `home/sections/featured-cars` (handler unreachable — imgs render in child shared `ui-select`, covered next), `sell/details-wizard` (2 branches, `w-8 h-8 text-[12px]`), and most impactful **`apps/web/src/app/shared/ui-select.component.ts`** (2 sites wrapped in `relative inline-block size-N shrink-0` span — covers Make/Model dropdowns on `/browse` + sell wizard + any future `<app-ui-select>` with `SelectOption.iconUrl`). **(d) v1.5-D3 NEW Customer CPO Inspection Report page** — `apps/web/src/app/features/sell/offer/cpo-inspection-report.component.ts` (371 lines, under 380-line cap, 13.98 kB lazy chunk). Lazy route `/offer/:token/inspection-report` added in `app.routes.ts`. "View inspection report" CTA wired in offer-page inside `@if (offerData(); as o)` block (strictly active/countered view; NOT on terminal states). `OffersService.getInspectionReport$(token)` returns discriminated union `loading | ok | not_found | expired | network_error` targeting `GET /v1/public/concierge/offers/:token/inspection-report`. **Brand-lock decisions**: mockup's green-for-good replaced with brand-blue bands — 90-100 `bg-brand-700 text-white`, 70-89 `bg-brand-100 text-brand-700`, 50-69 `bg-brand-50 text-brand-700 border border-brand-200`, <50 `text-red-600` (honest defect signaling). Section bars `bg-brand-500`. Advisory items neutral `bg-slate-200 text-slate-700`; fail items `bg-red-100 text-red-600`. **i18n**: 1 `sell.offer.viewInspectionReport` + ~32 `sell.offer.inspectionReport.*` keys (title, score bands, 6 category names with Arabic civic translations, attention items, 3 terminal states), EN+AR symmetric, parity guard PASS. **Verification matrix**: `nx build web --skip-nx-cache` PASS (12.4s, new chunk 13.98 kB); `guard:brand-lock` ✅ no violations post-extension; `guard:brand-lock-mobile` ✅ 162 files; `guard:i18n-parity` ✅ EN/AR synced (~33 new keys); `guard:i18n-parity-mobile` ✅ 736/736; `guard:secrets` ✅. **One new `[ASK A→B]`** for thin `GET /v1/public/concierge/offers/:token/inspection-report` (~30 lines, pivots offer→inspection via existing `toPublicSummary()`) — A side renders `not_found` gracefully until B ships. Posted v1.5-D contract block to CONCIERGE_INSPECTION_API_CONTRACT.md. Sell-car customer flow now visually + functionally complete end-to-end.
- **2026-05-21 C v0.17 — pre-dev-safe quad** `[SHIPPED 2026-05-21 C v0.17]` — 4 surfaces shipped in parallel, no external integrations touched (Otto/Apple/APNs all remain mocked per B v1.5.1 stakeholder lock): **(a) Documents wire** — `MeDocumentsApiClient` in `libs/data-access-mobile/src/lib/me-documents.client.ts` (Zod-parsed list + getDownloadUrl, mirrors MeInspectionsApiClient pattern) + `/documents` list route (252 lines, 6 states, useInfiniteQuery, tap calls getDownloadUrl(id) then expo-web-browser.openBrowserAsync per A's web pattern) + 3 sub-components (`DocumentListItem` 178 lines with MIME-aware glyph, `DocumentListStates` 156, barrel) + Stack.Screen + account-hub tile fix (was Coming-Soon → `/documents`) + 18 `documents.*` i18n keys EN+AR symmetric. **(b) Saved Searches wire** to B v1.5.3 backend — `MeSavedSearchesApiClient` (5 methods list/getById/create/update/delete, Idempotency-Key on create) + `/saved-searches` route (265 lines, 6 states, Run navigates to browse with fromBackendPayload filters, Delete with confirm) + 6 sub-components (SavedSearchListItem, SavedSearchListStates, DeleteConfirmModal, SaveCurrentSearchModal, queryPayloadTransform, index) + **mobile-side snake_case↔camelCase queryPayload transform** (lossless round-trip for BrowseFilters fields; backend-only fields pass through on save, silently dropped on load — no schema coupling with A's web) + browse "Save search" CTA pill (visible when activeFilterCount > 0; browse.tsx 474→486 lines, still <500) + Stack.Screen + account-hub tile fix + 23 `savedSearches.*` i18n keys. **(c) Brand-lock-mobile guard** — new `scripts/guard-brand-lock-mobile.mjs` (scans `apps/mobile/{src,app}/**/*.{ts,tsx}` for off-brand hex palettes + bare color names in style contexts; whitelists colors.ts; mirrors A's web guard pattern) wired as `npm run guard:brand-lock-mobile` and added to `guard:all`; cleanup landed: removed 3 unused legacy semantic palette constants (successGreen/warningAmber/infoBlue) from theme.ts — none were component-referenced. Guard exits 0 across 159 mobile files. **(d) `[ASK C→B] notifications-inbox`** posted to MOBILE v0.17 §1 with full endpoint spec (5 endpoints: list/unread-count/read/read-all/delete) + DTO sketch (channel/category/title/body/deepLink/iconHint/readAt/etc.) + error codes (`NOTIFICATION_NOT_FOUND`) + storage hint for B per standing offer. **Bonus carry-over closure**: B v1.5.4 (sister-shipped today) added `inspectionReportId` to PublicOfferViewSchema — shared-types rebuilt; offer view.tsx line 98 mock-ref now has TODO comment pointing at the new field path (full wire-up requires building OffersPublicApiClient first; tracked as next pre-dev-safe pick). Final locales: **EN 733 / AR 733 across 18 namespaces** (+ `documents` 18 keys, `savedSearches` 23 keys). `npx tsc --noEmit -p apps/mobile/tsconfig.json` exits 0. All 4 guards green (brand-lock + brand-lock-mobile + i18n-parity + i18n-parity-mobile). Mobile session idle pending B's notifications-inbox spec reply + Otto/APNs gates.
- **2026-05-21 B v1.5.5 + v1.5.4** `[SHIPPED 2026-05-21 B v1.5.5]` — response cycle to A v1.5-A/A4/B + C v0.16 carry-over close. **v1.5.4** (in-thread sister-ship): added `inspectionReportId: z.string().uuid()` to `PublicOfferViewSchema` in `libs/shared/types/src/lib/offer.schemas.ts` + populated from `row.inspection.id` in `getOfferByToken` (apps/api/src/offers/offers.service.ts:382, no-cost field add — `OFFER_INCLUDE` already pulls inspection row). Both `nx build {shared-types,api}` GREEN. Closes C v0.16 §carry-over — C can now replace `/inspections/test-inspection-id` mock ref in `apps/mobile/app/offers/[token]/view.tsx` with real `offer.inspectionReportId`. **v1.5.5 to A**: ACK A's clean Saved Searches landing (zero `saved-search-fixup` needed, snake_case worked, v1.5-A4 nest-bug catch was textbook §13.3); ACCEPT §13.5 proposal in principle with 3 amendments (migration-filename-in-shipped-block critical, B-side curl-probe required on new endpoints, type-only changes get short-form verify); A to draft canonical text in CONVENTIONS.md, B will ACK + add corresponding §13.2 amendment. **v0.16-B-reply to C**: ACK Inspections list wired clean; surface v1.5.4 with 4-step closeout (rebuild shared-types + edit view.tsx 1-line + restart Expo + smoke); flag operational verify needed per §13.5 even for type-only changes (API restart + smoke probe); Android push E2E offer still standing. **Lesson saved for future**: build PASS ≠ feature shipped — must verify migration applied + API restart + smoke probe before [SHIPPED]. B current: still idle, no new asks introduced.
- **2026-05-21 A v1.5-B — Saved Searches end-to-end smoke PASS** `[SHIPPED 2026-05-21 A v1.5-B]` — Full create loop verified live via Chrome MCP: applied BMW filter on /browse → "Save this search" CTA appeared in chips row → modal opened → typed "All BMW cars" → POST 201 → modal closed → /account/saved-searches lists the row w/ View results (brand-700) / Rename / Delete (red) / Notify toggle. **In-thread fix v1.5-A4**: "Save this search" CTA was nested inside `@if (activeChips().length > 0)` which excluded URL-param-applied filters (no chips populated). Moved outer guard to `@if (hasActiveFilters())`. Build PASS. **Operational gap surfaced**: v1.5-A claimed [SHIPPED] on build PASS alone — but actual operational state (migrations + API restart) was not verified, causing user's initial "Something went wrong" error. Proposed CONVENTIONS §13.5 — backend-wire ship-checklist extension requiring curl-probe + migrate-status + 1-step Chrome MCP smoke before stamping [SHIPPED] on any new B-endpoint wire. **Migrations applied**: `v1_3_7_pii_columns` (B's PII queue, GREEN since v1.4.5) + `v1_6_saved_searches`. No further user gates for v1.5-A/B. v1.5-B contract block posted to CONCIERGE.
- **2026-05-20 A v1.5-A — v1.6 Saved Searches customer surface wired** `[SHIPPED 2026-05-20 A v1.5-A]` — First v1.5/v1.6 customer-side delivery. 3 ruflo agents in parallel against B's just-shipped 5 SavedSearch endpoints: (1) NEW `data/saved-searches.service.ts` 172 lines — 5 methods (list/getById/create/update/delete) + 5 state unions + Zod validation + Idempotency-Key on POST/PATCH + `SAVED_SEARCH_NOT_FOUND` mapped on getById/update/delete only. (2) NEW `features/account/saved-searches-page.component.ts` 5-state list page (loading/ok/empty/error/pagination) + per-card actions: View results with query params, Rename inline, Delete confirm, Notify-on-match optimistic toggle. EDIT coming-soon-shells.ts to remove SavedSearchesShellComponent + EDIT app.routes.ts swap to real page. (3) NEW `features/browse/save-search-modal.{service,component}.ts` + EDIT browse-page.component.ts (`hasActiveFilters()` computed + CTA + BrowseFilters→SavedSearchQueryPayload mapping with `bodies→body_types`, `transmission→transmissions`, `fuel→fuel_types`, `inspected→inspection_flag`, KWD price ×1000→fils, year/mileage direct, sparse) + EDIT shell.component.ts to mount modal globally. ~45 new i18n keys merged EN+AR symmetric across 3 namespaces (`account.savedSearches.*` 28, `browse.saveSearchCta` 1, `savedSearches.modal.*` 16). All `nx build {shared-types,web}` GREEN, all 3 guards GREEN. §13 ship-checklist compliant. Zero `[ASK A→B] saved-search-fixup` needed — shared-types matched exactly.
- **2026-05-20 C v0.16 — Inspections list wired** `[SHIPPED 2026-05-20 C v0.16]` — `[ACK]` B's v0.15-B-roadmap §2 correction that `GET /v1/public/me/inspections` has been live since Sprint 3 (task #8). C v0.15 §7 wrongly deferred it. **NEW** `MeInspectionsApiClient` in `libs/data-access-mobile/src/lib/me-inspections.client.ts` (mirrors `OrdersPublicApiClient` pattern from #65; Zod-validated; wraps authenticated `httpClient`). **NEW** singleton `meInspectionsApiClient` in `apps/mobile/src/services/http.ts`. **NEW** list route `apps/mobile/app/inspections/index.tsx` (221 lines, useInfiniteQuery, pull-to-refresh, 6 states mirroring `/orders/index.tsx`) + 3 sub-components under `apps/mobile/src/components/inspections/list/*`: `InspectionListItem.tsx` (172 lines, vehicle title / `vinMasked` / mileage / status pill / scheduled date / latestOffer summary), `InspectionListStates.tsx` (154), `InspectionStatusPill.tsx` (69, maps the 5 `InspectionStatus` enum values to brand-only pill colors). **Account hub Inspections tile repointed** from mock `/inspections/test-inspection-id` → real `/inspections`. **Stack.Screen** for `inspections/index` registered in `_layout.tsx`. **i18n extended**: 17 new keys under existing `inspection.*` namespace (`inspection.list.*` 11 keys + `inspection.statusPill.*` 6 keys for draft/in_progress/awaiting_inspector_signoff/awaiting_customer_signature/signed_off + a11y). Final locales: **EN 694 / AR 694 symmetric**, 16 namespaces, `npm run guard:i18n-parity-mobile` exits 0. Two inspection clients kept deliberately separate: `InspectionsPublicApiClient` (no-auth, raw axios — sign-token route) vs `MeInspectionsApiClient` (auth, intercepted httpClient — /me list). Don't merge — no-auth invariant on sign-token is load-bearing. **Implementation note**: agent socket-crashed at 41 tool uses (transport error, partial response) but core landed (client + route + 3 components + http.ts singleton); C lead finished 4 trailing edits in-thread (account tile fix + Stack.Screen + 17-key i18n extension across en.json+ar.json). Same recovery pattern as Phase 2's stream-timeout in v0.15. **Carry-over**: `apps/mobile/app/offers/[token]/view.tsx` still has a `/inspections/test-inspection-id` mock ref on the "View Inspection" CTA — needs real id once offer schema exposes related inspection UUID. `npx tsc --noEmit -p apps/mobile/tsconfig.json` exits 0. Mobile session fully idle again. v0.16 contract block posted to MOBILE_API_CONTRACT.md.
- **2026-05-20 B v1.5.3 — Saved Searches backend shipped** `[SHIPPED 2026-05-20 B v1.5.3]` — pre-emptive build of v1.6 Saved Searches backend so A can wire UI without waiting. Background `saved-searches-backend` swarm (sonnet, 36 tool calls, 4.9 min wall, both nx builds GREEN). Files shipped: SavedSearch Prisma model (+38 lines in schema.prisma with User.savedSearches relation + composite index `[userId, createdAt(sort: Desc)]`) + migration `20260604000001_v1_6_saved_searches/migration.sql` (33 lines, hand-authored SQL aligned with Prisma decorators per memory `feedback_migration_drift`) + `libs/shared/types/src/lib/saved-search.public.schemas.ts` (107 lines, exports SavedSearchDto + SavedSearchListResponse + CreateSavedSearchInput + UpdateSavedSearchInput + SavedSearchQueryPayloadSchema; barrel re-exported via index.ts) + `apps/api/src/saved-searches/saved-search.service.ts` (145 lines) + saved-search.controller.ts (131 lines) + 2-line route mount in app.ts. **5 endpoints LIVE**: GET list (paginated) / GET one / POST create / PATCH update / DELETE — all auth-protected, all return locked `SAVED_SEARCH_NOT_FOUND` envelope on 404. **Schema decision flagged for A**: queryPayload keys are snake_case (year_min, price_min_fils, body_types, etc.) per brief instruction to match A's URL query convention — if A's internal filter object is camelCase, transform-on-save/load or post [ASK A→B] saved-search-key-convention-revisit and B will patch. **Migration awaits user**: `npm run prisma:migrate` (or `npx prisma migrate deploy`) — A can wire UI without waiting, only DB-touching tests need it. Posted v1.5.3 block to CONCIERGE with full endpoint + DTO + import patterns + future v1.6+ candidate items (new-matches cron + run-now preview).
- **2026-05-20 B v1.5.2-roadmap** `[SHIPPED 2026-05-20 B v1.5.2]` — backend-surface inventory + ranked next-pick instructions posted to both A (CONCIERGE) and C (MOBILE). **Important callout to C: `GET /v1/public/me/inspections` already exists (shipped Sprint 3 task #8)** — C v0.15 §7 had deferred this thinking it was missing. Mobile can wire the real list today, no B dep. Same for `GET /v1/public/me/documents` (live with signed S3 URLs, mobile tile still Coming-Soon). **For A: 4 zero-blocker recommendations** (i18n parity audit / date-i18n helper / bundle budget / signal-writes audit done) + **2 coordination-ready options** (Saved Searches with B offer to spec endpoints same-day, Returns module §3.21 as v1.5 substitute since Payments parked). **For C: 4 zero-blocker recommendations** (wire real inspections endpoint / Android push E2E now Firebase live / wire Documents endpoint / perf-bundle) + **3 coordination-ready options** (Notifications inbox / Maintenance Pickup §3.22 capture-only / Trade-In §3.19 capture-only). **B's standing offer to both**: post `[ASK A→B]` or `[ASK C→B]` with rough shape for any of the coordination items and B will spec + ship same session. Only Otto/PACI/Insurance/APNs categories remain parked.
- **2026-05-20 B v1.5.1-clarify** `[SHIPPED 2026-05-20 B v1.5.1]` — stakeholder scope-lock clarification across all 3 coordination files. **ON HOLD pending external API creds** (per user directive): (1) **Documents approval queue** — already dropped from v1.5; reaffirmed parked, (2) **Customer KYC** — already deferred pending PACI; reaffirmed parked, (3) **Insurance module** (SRS §3.18) — confirmed not in v1.5 scope, (4) **Otto payment integration** — newly parked; this means v1.5 Payments mockup approval + Angular + backend swarm all wait until Otto creds land. **Firebase confirmed FULLY SHIPPED B-side** — audit: `firebase-admin@^13.10.0` in package.json + `node_modules/firebase-admin/lib` present + `apps/api/.secrets/firebase-admin.json` (2379 bytes, gitignored) + `.env` `FIREBASE_SERVICE_ACCOUNT_PATH=apps/api/.secrets/firebase-admin.json` + `push.adapter.ts` lazy init `mod.initializeApp({ credential: mod.credential.cert(env.FIREBASE_SERVICE_ACCOUNT_PATH) })`. `hasFcmCreds()` auto-switches mock→real on first FCM dispatch — no code change needed when first push fires. **APNs (.p8 + KEY_ID + TEAM_ID + BUNDLE_ID)** still pending separately for iOS push. Gates table updated: Firebase ✅ CLEARED, APNs ⏳ pending, new `[ON-HOLD]` row for the 4-item parked list. Posted **B v1.5.1-clarify** to CONCIERGE_INSPECTION_API_CONTRACT.md (A-side acknowledgment + refund-status DTO deferred to whenever Otto unblocks) and **B v0.15-B-reply** to MOBILE_API_CONTRACT.md (C-side acknowledgment + FCM dispatch ready for #64 E2E test on Android side, iOS still gated). B effectively idle for v1.5 admin work until any of Otto / PACI / APNs / explicit unpark lands.
- **2026-05-20 C v0.15 — web-parity catch-up** `[SHIPPED 2026-05-20 C v0.15]` — `[ACK]` A v1.4.12 §8 hand-off + B v0.14 PACI/Documents implications. Three parallel ruflo agents shipped: **G1 Reserve flow** (opus, 409-line route + 3 companion files in `src/components/reserve/*` — full state machine mirroring A's v1.4.11 web checkout-modal, real `ordersPublicApiClient.create` + `.initPayment`, `expo-web-browser.openBrowserAsync` for Otto hosted-checkout, routes through `/orders/[id]/payment-return` on dismiss, 4 payment-method picker with Apple Pay + Google Pay locked Coming-Soon, all 8 ORDER_ERROR_CODES mapped; VDP ReserveStickyCTA now `router.push('/reserve/:id')`; Stack.Screen registered with `presentation: 'modal'`; step nested route deleted). **G2 Live reservation countdown** (`ReservationCountdown.tsx` new, every-1s setInterval with cleanup, only renders for `reservation_pending`/`payment_pending`, turns `red[500]` when < 1h, "Reservation expired" at 0; mounted above OrderSummaryCard). **G3 Cancel modal refund copy** + full Orders i18n sweep (`CancelConfirmModal` gained `reservationFeeFils` prop + refund line via `t('orders.cancel.refundCopy', { amount: formatKwd(fee) })`; all 13 Orders files swept of hardcoded English, zero `// TODO (i18n Phase 2)` comments remain). Two new i18n namespaces: `checkout.modal.*` (33 keys) + `orders.*` (75 keys). Final locales: **EN 677 / AR 677 across 16 namespaces** (+ `checkout`, `orders`). **§13.3 audit found + fixed in-thread**: account-hub Orders tile was wired Coming-Soon (`pill={<ComingSoonPill />}` + `onPress → /auth/sign-in`) despite #65 shipping `/orders` — now repointed to `/orders` with pill removed. Caught before any smoke walk. **New CI hygiene**: `npm run guard:i18n-parity-mobile` script + `scripts/check-i18n-parity-mobile.mjs` (mirrors A's web pattern; mobile session commits to running pre-merge on every locale-touching change). `npx tsc --noEmit -p apps/mobile/tsconfig.json` exits 0. Mobile session **fully idle**; all v1.4 mobile lifecycle work done at full feature parity with web. Only inbound gates remain: Otto sandbox creds (user→B) + APNs `.p8` + Firebase server key (user→B). User decision flagged: pre-launch `bundleIdentifier`/`package` alignment (`com.behbehani.cpo` → `com.behbehani.motors`).
- **2026-05-20 C v1.4 mobile + #69 closeout** `[SHIPPED 2026-05-20 C]` — **all mobile tasks done**: (1) **#64** push notification tap → deep-link routing wired. New `src/notifications/notificationRouter.ts` (3 exports + foreground/background listener) + scheme guard `isValidCustomSchemeUrl` in `services/deeplinks.ts` (rejects any URL not starting with `behbehani-motors://`) + cold-start handler in `_layout.tsx` via `getLastNotificationResponseAsync` with `cancelled` unmount guard. Two `useEffect` blocks added to RootLayout. (2) **#65** Orders flow shipped via opus: 3 new routes (`/orders`, `/orders/[id]`, `/orders/[id]/payment-return`), 10 sub-components under `src/components/orders/*` (StatusPill, OrderListItem, OrderListStates, OrderDetailHeader, OrderSummaryCard, PaymentSummaryCard, StatusTimeline, VehicleCard, CancelConfirmModal, OrderActionRow) + utilities (formatKwd, maskVin, formatDate, status pill style, newIdempotencyKey) + `useOrderPolling` hook with staged cadence (3s for first 60s, 10s for next 5min, stop at 6min OR terminal status) + 409 cancel-race handler (`onError` checks `axios.isAxiosError && response?.status === 409 && error.code === 'ORDER_NOT_CANCELLABLE'`, awaits refetch, shows Alert, OrderActionRow re-renders with cancellable=false). New `OrdersPublicApiClient` in `libs/data-access-mobile`, instantiated on httpClient (intercepted, auth + 401-refresh). Every file < 500 lines. (3) **#69** i18n Phase 2 sweep via **4 parallel chunked agents** (after Phase 2's initial single-agent stream-timeout): `offers.*` 72 keys (5 screens, 72 t() calls), `inspection.*` 38 keys (11 components + bouncer, 38 t() calls), `sell.*` ~69 keys (~10 components + 6 KW governorates with formal civic Arabic "محافظة العاصمة" etc. + 3 time windows), `account.*` 54 keys (8 components, 4-group hub IA). Final state: **EN 568 / AR 568 keys symmetric across 14 namespaces** (account, app, auth, browse, common, filter, home, inspection, listings, nav, offers, sell, sort, vdp). Locales 63 → 676 lines each. Arabic quality preserved (formal customer register, proper diacritics). All Phase 1 keys sacred — no regressions. **TSC clean across all 3 closeouts**. Mobile session has zero open tasks; only inbound gates are Otto sandbox creds + APNs/Firebase from user (both unlock end-to-end smoke testing of code that already exists).
- **2026-05-20 B v1.5.0-scope** `[SHIPPED 2026-05-20 B v1.5.0]` — v1.5 admin scope decisions + 3 mockups + audit fix-swarm + governance doc. Stakeholder-confirmed scope cuts: **(1) KYC review queue DEFERRED** pending PACI (Public Authority for Civil Information) integration channel selection — stakeholder pursuing direct PACI lookup to auto-populate 14 PII fields instead of manual admin review; queue reframes as exception-only fallback once PACI channel selected. **(2) Documents approval queue DROPPED from v1.5** — KYC docs PACI-sourced later + system-generated PDFs (receipt/sale_contract from v1.4.7) don't need approval; revisit in v1.6+ alongside Loan (§3.7 bank statements) or Dealer (§3.9 business licenses) modules when there is actual content to approve. **(3) Payments reconciliation IN SCOPE v1.5** — fully covered by SRS §3.21 + §6.3, Otto Payment Services as v1.4 aggregator, mock-mode until Day 5 creds. Portal-aligned mockup at `apps/admin/.mockups/v1.5-payment-reconciliation.html` (Tailwind CDN with brand palette injected, full admin shell + sidebar with Payments highlighted under Finance + topbar + 4 summary tiles + reconciliation banner + status chips + filters + 8-row payments table + detail drawer with Otto webhook timeline + refund modal with amber mock-mode warning banner). **Design baseline shipped** at `apps/admin/.mockups/DESIGN-BASELINE.md` — 15 sections of copy-pasteable Tailwind patterns extracted from actual shipped admin (orders-list, users-list, admin-shell) so future mockups visually match production. **Governance doc** at `docs/SRS_EXTENSIONS_v1.5.md` — 9 sections, 20+ SRS line citations across §1.3 / §3.7 / §3.9 / §3.12 / §3.17 / §3.21 / §4.3 / §4.4 / §6.3 / §6.4, records Extensions A+B as deferred and C as in-scope with approval ledger. **Audit fix-swarm**: 2 HIGH shipped — Customer Documents got route-level `adminRoleGuard(['super_admin','general_manager','customer_support'])` in `app.routes.ts` + `admin-auth.guard.ts` (was previously accessible to any authenticated admin); Save Draft button on `listing-edit.component.html:423` now checks `form.pristine` (was firing no-op saves). 4 MEDIUM deferred with rationale. `npx nx build admin` GREEN 17.6s. **3 open Payments-product decisions** before backend implementation: (a) partial-refund modeling — recommend negative-amount Payment row for §6.3 double-entry ledger compliance vs `refundedAmountFils` column, (b) reconciliation cadence — manual button + 15min cron (both, mockup shows both), (c) KWD partial-refund rounding — banker's rounding (round-half-even) default. **A+C impact** see CONCIERGE v1.5.0 + MOBILE v0.14 blocks.
- **2026-05-20 C #68 i18n Phase 1** `[SHIPPED 2026-05-20 C]` — large i18n sweep on the 4 customer-entry surfaces (sign-in / home / browse + filter / VDP) + shared `ListingCard`. **41 files modified, ~265 `t()` calls added across these subtotals**: auth ~54 (sign-in + 7 auth components), home ~30 (8 home components), browse + filter ~75 (browse screen + 6 browse components + FilterSheet + 9 filter pickers), VDP ~100 ([slug] + 13 vdp components), ListingCard 8. **267 new keys** across 5 new top-level namespaces (`browse.*`, `filter.*`, `home.*`, `sort.*`, `vdp.*`) plus extensions to `auth.*` (17→60 keys), `common.*` (11→17), `listings.*` (richer badge subtree). en.json + ar.json grew 63→357 lines each. **EN↔AR parity verified**: 331 leaf keys in each file, 0 missing, 0 extra (via `node -e` parity script from project root). Arabic translations are natural with proper diacritics (e.g. `مرحبًا بعودتك`, `جارٍ التحميل…`, `نسيت كلمة المرور؟`) — not transliterated. `npx tsc --noEmit -p apps/mobile/tsconfig.json` exits 0. **Phase 2 deferred to #69** (offers/inspections/sell/account screens — not blocking B's firebase work). Closes the §13.3 ship-checklist gap on the primary entry flows pre-v1.5 iOS.
- **2026-05-20 C #40 svg icons** `[SHIPPED 2026-05-20 C]` — `react-native-svg@15.8.0` installed (Expo SDK 52 compatible, version picked by `npx expo install`). Replaced placeholder text-glyph/emoji icons across three explicitly-TODO'd files: (1) **`vdp.icons.tsx`** — 15 exports rewritten as Feather-style react-native-svg icons (24x24 viewBox, stroke-width 2, round caps/joins): ShareIcon (was ↗), HeartIcon (♥/♡, filled prop preserved), PlayIcon (▶), ShieldIcon (🛡), WarrantyIcon (🔒), ReturnIcon (↩), TruckIcon (🚚), CheckIcon (✓, size/color preserved), CreditCardIcon (💳), OfferIcon ($ glyph), CalendarIcon (📅), LoanIcon (🏦), ChatIcon (💬), PhoneIcon (📞), CarSilhouette (🚗). All 15 exports keep identical names + signatures — callers across `app/listings/[slug].tsx` + 8 `vdp/*` components unaffected. (2) **`OverallScoreCard.tsx`** — 112x112 border-ring placeholder replaced with `Svg` + 2x `Circle` arc (strokeDasharray/strokeDashoffset). Track slate[200], progress brand[700] when score≥80 (CPO threshold) else brand[600]. (3) **`InspectionReportEmbed.tsx`** — 56x56 border + rotated-arc trick replaced with same `Svg` + 2x `Circle` pattern; track brand[100], progress brand[800]. Removed stale TODOs + unused gauge styles. `npx tsc --noEmit -p apps/mobile/tsconfig.json` exits 0. **Out of scope** (intentional): LanguageToggle 🇰🇼/🇬🇧 flags, SignInForm 🔒/👁, ListingCard ♥/♡, sell/account/offers/browse/home decorative emoji — those are skin choices, not placeholders.
- **2026-05-20 C W2 closeout** `[SHIPPED 2026-05-20 C]` — 5 W2 follow-up debts cleared in one pass post-v0.13: (1) **#62** `inspections/[id].tsx` split 1158→107 lines via 13 `src/components/inspections/*` components (mirrors vdp/sell pattern; VIN last-6 mask + KWD 3-decimal + RED_500-only-on-fail + ≥44px touch targets all preserved). (2) **#44** `handleSignIn` rewritten: real `AuthService.signIn({ type: 'email', ... })` call, double-submit guard via `loading` state, `router.replace('/(tabs)')` on success, error envelope parsed for `ACCOUNT_LOCKED` vs `INVALID_CREDENTIALS`. SignInForm gains `loading?: boolean` → button disabled + label swaps to "Signing in…". (3) **#43** queryClient singleton extracted to `apps/mobile/src/services/queryClient.ts` — http.ts + _layout.tsx now share the same instance, so the TOKEN_REUSED `clear()` actually evicts cache entries the UI reads (was silent no-op pre-fix). (4) **#47** canonical `ListingCard` wired in browse grid/list (replaces local `ListingCardPlaceholder` — price-drop strikethrough + favorite heart behavior consistent home↔browse). (5) **#49** `as const` typed-route cast on `ListingCard.tsx:153` normalized to `as any` matching the 8 other dynamic router.push sites. `npx tsc --noEmit -p apps/mobile/tsconfig.json` exits 0. Mobile idle pending operational gates — only #40 (svg cosmetic) + #68 (i18n sweep pre-v1.5 iOS) remain non-blocked.
- **2026-05-20 A v1.4.9** `[SHIPPED 2026-05-20 A v1.4.9]` — signed-in smoke walk: 6/10 surfaces PASS (sign-in modal w/ logo+965, hub 13-tile, profile cards + **password meter FIX confirmed live (4 bars→Strong, Update btn enabled)**, notifications **toggle cross-talk FIX confirmed live (only clicked cell flipped)**, documents real page, orders real page). **1 P1 fixed inline**: `addresses.component.ts` effect cycle (read+write `pageState` in same effect → renderer freeze) → wrapped read in `untracked()`. **1 P2**: `account.notifications.dirtyHint` i18n key missing. **1 P3**: profile hero still full-bleed vs other sub-pages rounded-card. 3 surfaces (security/my-bookings/saved-cars) deferred — re-walk after dev-server hot-reload. `nx build web` GREEN.
- **2026-05-20 B v0.13-B-reply** `[SHIPPED 2026-05-20 B v0.13-B-reply]` — parallel server-side scheme parity audit after C's v0.13 §13.3 mobile audit. Caught matching B-side bug: `order.service.ts:466` was emitting web-relative `/account/orders/${id}` for push `deepLink` instead of full `behbehani-motors://orders/${id}` mobile URL (per MOBILE_API_CONTRACT.md §4). Push routing would have failed to open order detail on tap — same blast radius as C's app.json scheme bug. Fixed; nx build api GREEN. Convention locked for future v1.4 Day 8+ push emissions. CONVENTIONS §13.2 self-audit on Days 1-7 done — only the deepLink gap found.
- **2026-05-20 C v0.13** `[SHIPPED 2026-05-20 C v0.13]` — `[ACK]` A's v0.12 (schema-3 closed via shared-types delete; OrderDTOs noted ready for task #65). §13.3 mobile audit caught 2 real bugs: (1) CRITICAL `app.json` scheme `behbehani-cpo` mismatched `behbehani-motors://*` deep-link URLs in MOBILE_API_CONTRACT §4 → push notifications would fail to route. FIXED across 5 files (app.json + deeplinks.ts code+docs + listing/[id] + inspections-public.client + ARCHITECTURE.md). Zero `behbehani-cpo://` references remain. (2) i18n EN+AR gap on recent screens (sell partial, offers/inspections 0 hits) — task #68 tracking retrospective sweep before v1.5 iOS. Mobile commits to §13.3 forward-compliance for every future agent spawn. TS clean.
- **2026-05-20 A v1.4.8** `[SHIPPED 2026-05-20 A v1.4.8]` — `/account/orders` real page wired against B's `GET /v1/public/me/orders` (replaces OrdersShellComponent; service + 374-line page with 6 states + ~30 i18n keys EN+AR symmetric). Brand-lock cleanup complete: 45 pre-existing violations across 9 files → 0 (`npm run guard:brand-lock` ✔). `[ASK C→A] schema-3` closed (orphan `device-token.public.schemas.ts` deleted + barrel cleaned + dist purged). `[ASK B→A] B-A-1` closed (CONVENTIONS.md §13 "Agent ship-checklist — menu wiring required" added). All `nx build {shared-types,web}` GREEN.
- **2026-05-20 C v0.11** `[SHIPPED 2026-05-20 C v0.11]` — `[ACK]` v0.10-B-reply (all 6 B-C asks answered). B-C-5 wired: `notifications.client.ts` now imports canonical `PushTokenInputSchema` (`.min(20).max(512)`) from `@behbehani-cpo/shared-types`; dropped inline `RegisterPushTokenSchema`. B-C-3 + B-C-8 confirmed no-op. B-C-4 (deep-link routing) + B-C-6 (Otto callback polling) + B-C-7 (409 cancel race) deferred to v1.4 mobile Day 2+ / Day 5+ / Day 8+ — tracked in tasks #64/#65 with full code patterns in v0.11 §2/§4/§5. New `[ASK C→A] schema-3` posted to delete orphan `device-token.public.schemas.ts`. TS clean.
- **2026-05-20 C v0.10** `[SHIPPED 2026-05-20 C v0.10]` — coordination spine adopted (STATUS.md row updated · tags glossary internalised · pre-emptive stub pattern accepted) · A-1 `previousPriceFils` wired in `ListingCard.tsx` (price-drop strikethrough now shows actual original price) · A-2 `PublicListingDetailDto` wired in `listings-public.client.ts` (dropped local `ListingDetail` interface · `vdp.types.ts` now aliases canonical DTO · 4 spec field renames absorbed: `photoUrls→photos[]`, `drivetrain→driveTrain`, `accidentFlag→accidentHistory`, `inspectionScore/Date/Categories→inspectionReport`). `npx tsc --noEmit -p apps/mobile/tsconfig.json` clean.
- **2026-05-20 C W2 refactor sweep (6/7 done)** `[SHIPPED 2026-05-20 C]` — 6 of 7 oversize files split: home/(tabs)/index 1009→204 (7 components + unified ListingRail) · sign-in 951→167 (8 components) · listings/[slug] 1885→240 (13 vdp/* components + types + helpers + icons) · browse 1135→280 + FilterSheet 853→245 (7 browse/* + 10 filter/* components) · account 945→270 (8 components + `expo-linear-gradient` hero) · sell 1306→215 (9 sell/* components + types + dateHelpers). `inspections/[id].tsx` (1158) refactor still in flight.
- **2026-05-20 B v1.4.7** `[SHIPPED 2026-05-20 B v1.4.7]` — v1.4.6 coordination spine adopted (STATUS.md + CONVENTIONS.md tags) + v1.4 Day 4 (Order + Payment + reservation cron + Listing.stage `reserved` was already-an-enum-value + admin Documents backend + admin Documents Angular UI at `/customers/:id/documents`) + v1.4 Day 6 (auto-receipt PDF via `pdfkit` wired into Otto callback success → Document `kind:'invoice'` + admin Orders backend 4 endpoints + admin Orders Angular UI at `/orders` + `/orders/:id`) + v1.4 Day 7 (sale_contract templated PDF via `@react-pdf/renderer` no-JSX, wired into admin updateOrderStatus `completed` → Document `kind:'sale_contract'`). All `nx build {shared-types,api,admin}` GREEN. 7 swarm agents + 1 stream-timeout fix-cycle + 1 TS narrow fix in-thread, ~31 min wall-clock total
- **2026-05-20 A v1.4.6** — coordination strategy bootstrap: `STATUS.md` + `CONVENTIONS.md` + dev test customer seed (`smoke@test.local`/`Smoke#2026`) + mockup-fidelity LOCKED registry (68 mockups) + 3 pre-commit guards (`scripts/guard-{brand-lock,i18n-parity,secrets}.mjs`) + mockup-diff structural script + Playwright visual-regression bootstrap (5 pages, workflow_dispatch only, baseline capture deferred)
- **2026-05-20 A v1.4.5** — account hub rebuild (mockup-fidelity) + 5 v1.4 fix agents (password meter, notifications toggle cross-talk, sub-nav consistency, my-bookings reskin, Documents page real) + 5 brand-lock fixes + Morad Yousuf Behbehani logo unified across 5 entry points + +965 alignment + C's A-1+A-2 absorbed into shared-types
- **2026-05-19 B v1.4.4** — v1.4 Day 1-3 (PushToken model + Document endpoints + NotificationService with FCM/APNs adapters + email/SMS wrappers) ~11min wall-clock
- **2026-05-20 C v0.8** — mobile sale-flow re-alignment to web v2 (3-step wizard mirror) + D1 compliance (zero "1 round" copy) + 5 offer-state screens + customer inspection-report viewer + deep-link bouncer
- **2026-05-19 A v1.3.7** — v1.3 customer-account surface Day 2+3 (5 pages + 8 Coming-Soon shells + i18n + apple disabled pill)
- **2026-05-19 B v1.4.3** — v1.4 kickoff confirmation + Day 1 head-start (cron infra + S3 doc + NotificationService skeleton)

---

## 🧪 Dev environment

### Test credentials (NODE_ENV !== 'production')
- **Customer** (signed-in surfaces): `smoke@test.local` / `Smoke#2026` (seeded via `npm run db:seed` — the package.json script name is `db:seed`, not `prisma:seed`)
- **Admin (CPO)**: `admin@test.local` / `Admin#2026` (existing seed)

### Local servers
- Web storefront: `npx nx serve web` → http://localhost:4200
- API: `npx nx serve api` → http://localhost:3000/v1/*
- Admin: `npx nx serve admin` → http://localhost:4201
- Mobile: `cd apps/mobile && npx expo start` (Expo Go on iOS/Android)

### Migration command
- `npm run prisma:migrate` (covers v1.3 + v1_3_feature_waitlist + v1.3.7 PII + v1.4 push-tokens + v1.4 documents + Day 4 orders-payments when shipped)

---

## 📁 Source-of-truth files (when STATUS.md is too compact)

- `CONCIERGE_INSPECTION_API_CONTRACT.md` (A↔B coordination, ~4500 lines, append-only versioned blocks)
- `MOBILE_API_CONTRACT.md` (C-side coordination, ~2000 lines, append-only)
- `V1_4_ROADMAP.md` (v1.4-v1.7 sprint planning, 11 subsystems catalogued)
- `mockups/LOCKED.md` (approved mockup registry — see v1.4-S3)
- `CONVENTIONS.md` (tag glossary + sync ritual — read this first)

---

## 🔄 Update protocol

End of every session block (~5 min):
1. Update **In flight** row for your session (working on / ETA)
2. Add anything you shipped to **Recently shipped**
3. Add/close items in **Open asks** + **Blocking**
4. Bump **Last updated** line at top
5. Commit STATUS.md changes alongside your code commit (no separate PR)

If you skip the update, the next session reading STATUS.md gets a stale picture. Treat it like a daily standup — short, current, true.
